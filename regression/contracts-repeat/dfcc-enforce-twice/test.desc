CORE
main.c
--dfcc main --enforce-contract foo _ --dfcc main --enforce-contract foo
^EXIT=6$
^SIGNAL=0$
^.*DFCC instrumentation has already been applied to this binary.*$
^.*Only one DFCC pass per binary is supported.*$
--
^.*VERIFICATION SUCCESSFUL.*$
This test enforces a contract under the DFCC pipeline twice on the
same goto binary. The second invocation must fail with the per-binary
DFCC error message.
