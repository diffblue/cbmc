CORE
main.c
--show-goto-functions --function skipWhitespace
no existing entry point
--
--
There is no entry point in the original goto functions so we don't know what
language to generate the new entry point in. This could probably be improved.