KNOWNBUG
main.cpp
--cpp11 --object-bits 12 --unwind 5 --no-unwinding-assertions
^\[main\.assertion\.1\] line 8 regex match: SUCCESS$
^VERIFICATION SUCCESSFUL$
^EXIT=0$
^SIGNAL=0$
--
--
std::regex construction + regex_match over libstdc++ <regex>.
CONVERSION is now complete and layout-independent (2026-08-12: the
type2name ctor/dtor throw and the raw-deferred-body leak are fixed;
zero demotions in 8/8 layout probes).  Remaining issue is SOLVER TIME
only: instantiation content still varies with allocator address order
(scope id-sets iterate in pointer order), so some layouts verify in
~90-300s and others exceed practical bounds -- with --slice-formula
--property --smt2, 4 of 7 probe runs finished under 15 min, 3 did
not.  Needs either order-canonical resolution (blocked on first-wins
consumers, cf. cpp17_std_function_lambda_call) or further solver-side
work; conversion-side the front end is done.
