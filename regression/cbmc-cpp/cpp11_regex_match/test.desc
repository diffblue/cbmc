CORE
main.cpp
--cpp11 --object-bits 12 --unwind 5 --no-unwinding-assertions
^\[main\.assertion\.1\] line 8 regex match: SUCCESS$
^VERIFICATION SUCCESSFUL$
^EXIT=0$
^SIGNAL=0$
--
--
std::regex construction + regex_match over libstdc++ <regex>.
Flipped KNOWNBUG->CORE 2026-08-11: the earlier symex crash
(lhs.type()==rhs.type() via _M_get_deleter) no longer reproduces after
the rounds 32-35 template-pack and overload-ranking fixes; 0 of 41987
properties fail, non-vacuous (1 assertion).  Runtime-verified with
g++ and clang++.
--object-bits 12 --unwind 5 --no-unwinding-assertions: full <regex>
conversion exceeds 2^8 objects; bounded harness.
