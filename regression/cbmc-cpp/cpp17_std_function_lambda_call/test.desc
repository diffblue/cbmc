CORE
main.cpp
--cpp17 --object-bits 12
^\[apply\.assertion\.1\] line \d+ handler ran through std::function: SUCCESS$
^VERIFICATION SUCCESSFUL$
^EXIT=0$
^SIGNAL=0$
--
^CONVERSION ERROR$
--
PARTIAL 2026-07-21: the forward<int&> no-body class is fixed
(cpp_type2name canonicalizes frontend references so instantiation
identities unify; a post-drain sweep finishes system-header instances
half-converted under candidate matching).  Remaining: _M_manager
dispatch imprecision (unconstrained function-pointer candidates,
bounds failures in ~_Function_base) keeps the invocation unverified.
Minimal header-free reproducer: cpp17_function_handler_dispatch.
Fixed by the round-6 template fixes (2026-08-03 re-test: non-vacuous
VERIFICATION SUCCESSFUL, 1 assertion; likely the recursive-member-
alias or zero-length-pack-expansion fix unblocked the _M_manager /
_Any_data layer).
ORDER-SENSITIVITY WARNING 2026-08-12: this test's conversion depends
on scope id-set iteration order (std::set<cpp_idt*> = allocator
address order).  Under a creation-ordinal (declaration-order)
comparator, _Base_manager::_M_get_pointer stops converting and this
test FAILS (no-body property) -- some first-wins resolution consumer
is tuned to address order by accident.  Any change to scope-set
ordering must re-validate here first; see findings log round 36.
