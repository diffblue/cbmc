CORE
main.cpp
--cpp11
^\[main\.assertion\.1\] line \d+ resolved: SUCCESS$
^VERIFICATION SUCCESSFUL$
^EXIT=0$
^SIGNAL=0$
--
--
g++/clang++ accept and the assertion holds natively.  See main.cpp
header for the candidate fix and its regressions.
Fixed 2026-08-07 (round 21), CONSUMER-side: a use of the TT-parameter
with its own template-argument list derives the TEMPLATE from the
bound instance at the resolve site; the deduction-side instance
binding (load-bearing unification) stays.
