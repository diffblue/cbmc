CORE
main.cpp
--cpp17
^\[apply\.assertion\.1\] line \d+ handler ran through std::function: SUCCESS$
^VERIFICATION SUCCESSFUL$
^EXIT=0$
^SIGNAL=0$
--
--
Fixed 2026-07-30 by the cpp17_pack_cast_fn_type_spec fix (convert
substituted types before constructor synthesis) plus the earlier
reference-cast candidates; the reproducer's null functor storage was
also repaired (static storage + default-constructible functor) so the
remaining CBMC diagnosis was a genuine bug in the test itself.
--
the concretized-pack family.
Remaining-layer minimal reproducer: cpp17_pack_cast_fn_type_spec.
