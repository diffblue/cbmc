KNOWNBUG
main.cpp
--cpp20
^\[main\.assertion\.1\] line \d+ friend default arg names member's own parameter: SUCCESS$
^VERIFICATION SUCCESSFUL$
^EXIT=0$
^SIGNAL=0$
--
--
Currently: main is dropped (VERIFICATION SUCCESSFUL is VACUOUS -- check
--show-properties, 0 assertions).  The innermost throwing frame is the
resolution of `Const` (verified with an uncaught_exceptions tracer on
resolve).  This is the ranges pipe's current blocker
(cpp20_ranges_pipe_invoke_drop), reduced to 44 lines.
