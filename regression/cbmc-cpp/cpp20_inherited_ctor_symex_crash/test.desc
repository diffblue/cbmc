CORE libcxx
main.cpp
--cpp20 --stdlib libc++
^\[main\.assertion\.1\] line \d+ pf: SUCCESS$
^VERIFICATION SUCCESSFUL$
^EXIT=0$
^SIGNAL=0$
--
--
symex invariant crash (lhs.type() == rhs.type()) on the inherited-
constructor chain; banked 2026-08-05 (round 13) while diagnosing the
ranges pipe residual.  clang builtins => CLANG mode.
Fixed 2026-08-05 (round 14): the partial-spec pattern re-deduction's
pack bindings are replayed into the active template map for the
instance's own body conversion, so the sizeof...-based static member
initializer resolves and no raw parse tree reaches goto conversion.
