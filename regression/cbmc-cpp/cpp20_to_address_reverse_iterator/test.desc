KNOWNBUG libcxx
main.cpp
--cpp20 --stdlib libc++
^\[main\.assertion\.1\] line \d+ to_address of reverse_iterator: SUCCESS$
^VERIFICATION SUCCESSFUL$
^EXIT=0$
^SIGNAL=0$
--
--
Currently VACUOUSLY successful: main's conversion throws (the
__to_address resolution cascade) and the body is truncated at the
failing statement with only the "could not fully type-check 'main'"
warning -- 0 assertions survive, so the SUCCESSFUL verdict is
meaningless until the front-end bug is fixed (the desc's assertion
line then guards against vacuity).  Verified natively under clang++
-stdlib=libc++ (assertion holds).
