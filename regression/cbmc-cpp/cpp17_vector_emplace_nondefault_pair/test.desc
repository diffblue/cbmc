CORE
main.cpp
--cpp17 --object-bits 12 --unwind 4 --no-unwinding-assertions
^\[main\.assertion\.1\] line \d+ emplace_back pair with non-default-constructible member: SUCCESS$
^VERIFICATION SUCCESSFUL$
^EXIT=0$
^SIGNAL=0$
--
--
Fixed 2026-07-27: the member-instance parameter pattern arrives with
its pack reference scalar-substituted to the first element; expansion
now falls back to the single live pack when the pattern is fully
concretized (member-function-template instantiations only).  See the
src commit for the [temp.variadic]/5 grounding.
