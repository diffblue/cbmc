THOROUGH smt-backend libcxx
main.cpp
--cpp20 --stdlib libc++ --object-bits 12 --unwind 3 --no-unwinding-assertions --slice-formula --property main.assertion.1 --smt2
^\[main\.assertion\.1\] line 12 erase_if: SUCCESS$
^VERIFICATION SUCCESSFUL$
^EXIT=0$
^SIGNAL=0$
--
--
std::erase_if over std::vector<int> (libc++).  Flipped
KNOWNBUG->THOROUGH smt-backend 2026-08-12: the harness assertion
verifies via the SMT backend (~6m15s with z3-checked equivalence;
solver time beyond the CORE budget).  The SAT backend's bit-blasting
runs out of memory (>50 GB) on this equation even for the single
property and unwind 3 -- a flattening scale limit, not a front-end
defect (the goto conversion is complete: 34k SSA steps, 1 VCC).
