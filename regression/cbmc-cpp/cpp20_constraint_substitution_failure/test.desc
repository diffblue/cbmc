KNOWNBUG
main.cpp
--cpp20
^\[main\.assertion\.1\] line \d+ unsatisfied constraint, not an error: SUCCESS$
^VERIFICATION SUCCESSFUL$
^EXIT=0$
^SIGNAL=0$
--
--
Update 2026-07-27: the requires-clause now PARSES (concept
template-ids; see cpp20_requires_clause_concept_template_id, CORE)
and the constrained candidate is correctly rejected -- but the
half-instantiated concept-variable instance left behind by the
genuine argument substitution failure (same_as<int,
common_reference_t<int,int>> with no ::type) poisons the SECOND
resolution pass: the whole statement is silently dropped (0
properties, vacuous SUCCESS caught by this desc's assertion-line
pattern).  Same silent-statement-dropping family as
cpp17_optional_requires_ctor_pair.
