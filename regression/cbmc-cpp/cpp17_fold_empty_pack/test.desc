KNOWNBUG
main.cpp
--cpp17
^\[main\.assertion\.1\] line \d+ empty && fold yields true: SUCCESS$
^\[main\.assertion\.2\] line \d+ empty \|\| fold yields false: SUCCESS$
^VERIFICATION SUCCESSFUL$
^EXIT=0$
^SIGNAL=0$
--
^CONVERSION ERROR$
--
KNOWNBUG: [expr.prim.fold]/3 -- a unary fold over an EMPTY pack must yield the
operator's identity (true for &&, false for ||), but instantiating the
function template with an empty pack loses the body entirely ("no body for
callee"), returning nondet.  g++/clang++ runtime-verified.  Flip to CORE once
the empty fold yields its identity.
