\documentclass[runningheads]{llncs}

\usepackage{booktabs}
\usepackage{multirow}
\usepackage{xspace}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage[algoruled,linesnumbered,vlined]{algorithm2e}
\usepackage{graphicx}
\usepackage{hyperref}

\newcommand{\combacs}{\textsc{comba-cs}\xspace}
\newcommand{\cadical}{\textsc{CaDiCaL}\xspace}
\newcommand{\cbmc}{\textsc{CBMC}\xspace}

\begin{document}

\title{Algebraic Solving for Bit-Vector Arithmetic\\in SAT-Based Decision Procedures}

\titlerunning{Algebraic Solving for Bit-Vector Arithmetic}

\author{Anonymous\inst{1}}
\authorrunning{Anonymous}

\institute{Anonymous Institution}

\maketitle

\begin{abstract}
Bitwuzla and cvc5 dispatch arithmetic identities via
pattern-matched rewriting and occurrence-map canonicalisation,
complete for commutativity and associativity but not for richer
polynomial identities; cvc5 additionally applies Gaussian
elimination modulo $2^d$ for linear equation systems. Neither
tool implements a decision procedure for nonlinear polynomial
identities over $\mathbb{Z}_{2^d}$, and on high-degree identities
and on DSP datapath equivalences with overflow cancellation both
rewriting and bit-blasting time out.

We give the first SMT integration of a complete decision
procedure for universally-quantified equalities of polynomial
expressions in $\mathbb{Z}_{2^d}$, combining a strong Gr\"obner
basis solver (ideal membership) with a vanishing-polynomial test
using falling factorials and Kronecker-product Stirling matrices
(function equivalence). On the 210-benchmark random-polynomial
sample of Brain~\cite{brain2025subpoly} the procedure decides 4
benchmarks at degree $\geq 13$ that Bitwuzla, cvc5, and every
bit-blasting configuration we evaluated time out on within
10\,s; on 5 DSP datapath equivalences with overflow
cancellation~\cite{shekhar2007equivalence,gamez2024efficient}
it solves in $<$15\,ms (cvc5 takes 1.84\,s on
\texttt{dsp\_horner\_16}, a 184$\times$ gap); on
SABER~\cite{daemen2018saber} polynomial multiplication
(post-quantum cryptography) the procedure scales to SABER's
actual $N=256$ parameter, with cvc5 timing out at every
$N \geq 4$; on commutativity, associativity, and overflow
checking it is bitwidth-independent (BW=8 to 256, 2--5\,ms
throughout) and matches Bitwuzla's word-level rewriting. Soundness is mechanised in
Lean~4 with 25 theorems and zero \texttt{sorry}, including a
contribution to Mathlib (under review); the formalisation effort
exposed and removed a 2000$\times$ ordering sensitivity in the C++
implementation that empirical testing did not catch.

\keywords{Gr\"obner basis \and bit-vector arithmetic \and
vanishing polynomials \and bit-vector SMT \and Lean 4.}
\end{abstract}

\section{Introduction}

A bit-vector SMT solver verifying
$\forall a, b \in \mathbb{Z}_{2^{16}}.\ a \cdot b = b \cdot a$ has
two operational options: recognise the identity \emph{syntactically}
via word-level rewriting (the approach taken by both
Bitwuzla~\cite{niemetz2023bitwuzla} and cvc5), or bit-blast both
sides into a SAT formula and refute the negation. The first is
fast when the rewriter has a matching pattern; the second is slow
but general. Both fail when the polynomial identity escapes the
rewriter's pattern set --- on
$(a + b)^{18} = \sum_{i=0}^{18} \binom{18}{i} a^i b^{18-i}$ over
$\mathbb{Z}_{2^{16}}$ no current SMT solver we tested closes the
goal within 10\,s, and on DSP datapath equivalences whose two
sides differ by a vanishing polynomial under modular overflow,
rewriting falls through to a bit-blasted formula that all four
SAT solvers we evaluated time out on.

We give a third path: a \emph{decision procedure} for
universally-quantified equalities of polynomial expressions in
$\mathbb{Z}_{2^d}$, complete in two senses (ideal membership and
function equivalence) and integrated as a pre-pass in
\cbmc~\cite{clarke2004tool}. The procedure decides such queries
in milliseconds, regardless of bitwidth.

\paragraph{Scope: four classes of bit-vector arithmetic query.}
Bit-vector queries with multiplication split by quantification
(universal vs.\ existential) and connective (equality vs.\ a
relation $\bowtie \in \{<, \leq, \neq\}$) into four classes. The
two papers in this development cover them as follows
(Table~\ref{tab:taxonomy}); this paper addresses the
universal-equational class with polynomial expressions in
$\mathbb{Z}_{2^d}$, and the bit-blasting companion
paper~\cite{companion-bitblasting} addresses the
universal-relational and existential-relational classes.
Existential-equational queries are split by direction:
SAT-direction (witness construction) is handled by bit-blasting
in both papers; UNSAT-direction (refutation) is handled in this
paper via the Rabinowitsch trick.

\begin{table}[ht]
\caption{Four classes of bit-vector arithmetic query, with the
example of multiplication, and the technique that decides each.
Existential-equational is split by direction: SAT (find a
witness) vs.\ UNSAT (no witness exists).}
\label{tab:taxonomy}
\centering
\small
\begin{tabular}{llll}
\toprule
Quantifier & Connective & Example & Decided by \\
\midrule
universal   & equational ($=$)        & $a \cdot b = b \cdot a$    & \textbf{this paper} (Gr\"obner / vanishing) \\
universal   & relational ($\bowtie$)  & $|a \cdot b| < 2^{30}$     & companion paper (bit-blasting) \\
existential & equational, UNSAT-dir.  & $\neg \exists x.\ p(x) = c$ & \textbf{this paper} (Rabinowitsch) \\
existential & equational, SAT-dir.    & $\exists x.\ p(x) = c$     & bit-blasting (witness) \\
existential & relational              & overflow witness           & companion paper (bit-blasting) \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Contributions.}
\begin{enumerate}
\item \textbf{A decision procedure for ideal membership in
  $\mathbb{Z}_{2^d}$.}
  A strong Gr\"obner basis solver over $\mathbb{Z}_{2^d}$, built
  on the theoretical foundation of
  Song et al.~\cite{song2024equational}. We add an SMT-grade
  implementation: SSA-aware decomposition of nested
  multiplications, the Rabinowitsch trick for disequalities, and
  a progress-based termination rule that eliminates
  equation-ordering sensitivity
  (Section~\ref{sec:algebraic}).

\item \textbf{A decision procedure for function equivalence over
  $\mathbb{Z}_{2^d}$.}
  Ideal membership in $\mathbb{Z}_{2^d}[x_1,\ldots,x_n]$ is not
  the right question for SMT: an SMT user verifying
  $\forall a, b.\ f(a,b) = g(a,b)$ wants \emph{function}
  equivalence, and over $\mathbb{Z}_{2^d}$ the two notions
  diverge --- e.g., $4x^2 + 4x \equiv 0 \pmod 8$ as a function
  but is non-zero as a polynomial. We give a vanishing-polynomial
  test using falling-factorial decomposition and Kronecker-product
  Stirling matrices, complete for the function-equivalence
  semantics, that decides such cases in microseconds
  (Section~\ref{sec:vanishing}).

\item \textbf{Mechanised soundness with formalisation-driven
  refinement.}
  25 theorems in Lean~4 with zero \texttt{sorry}; the unit
  characterisation of $\mathbb{Z}_{2^d}$ is contributed to
  Mathlib (under review). The Lean termination proof forced us
  to isolate a ``progress'' invariant that, when ported back to
  the C++ implementation, eliminated a 2000$\times$ ordering
  sensitivity that empirical testing had not exposed
  (Section~\ref{sec:algebraic-algo}).
\end{enumerate}

\paragraph{Empirical results.}
On the 210-benchmark random-polynomial sample of
Brain~\cite{brain2025subpoly}, the procedure decides 4
benchmarks at degree $\geq 13$ that Bitwuzla 0.9.0-dev,
cvc5 1.3.3, and every bit-blasting configuration we tested time
out on within 10\,s (Section~\ref{sec:eval-random-poly}). On 5
DSP datapath equivalences with overflow
cancellation~\cite{shekhar2007equivalence}, the procedure solves
all five in $<$15\,ms; cvc5 takes 1.84\,s on
\texttt{dsp\_horner\_16}, a 184$\times$ gap
(Section~\ref{sec:eval-dsp}). On
SABER~\cite{daemen2018saber} polynomial multiplication ---
a post-quantum cryptography application --- the procedure
scales to SABER's actual $N = 256$ parameter, deciding each
output-coefficient equivalence in $\sim$109\,s, while cvc5
times out at every $N \geq 4$
(Section~\ref{sec:eval-saber}). On commutativity, associativity,
and overflow checking the procedure is bitwidth-independent
(2--5\,ms from BW=8 to 256), matching Bitwuzla's word-level
rewriting and outpacing pure bit-blasting (which times out at
BW=16). On the same random-polynomial sample, Bitwuzla
solves 184/210 to our 128/210: its
occurrence-map canonicalisation closes most queries
syntactically and constructs witnesses for the
existential-equational SAT-direction queries that our
UNSAT-oriented Rabinowitsch encoding cannot. Our wins are the
queries Bitwuzla cannot decide (the four random-polynomial
high-degree wins) or that cvc5 cannot decide (DSP datapath
equivalences and SABER polynomial multiplication).

We introduce zero correctness regressions across \cbmc's 691
existing regression tests.

\section{Algebraic Solving via Gr\"obner Bases}
\label{sec:algebraic}

This section presents the \emph{ideal-membership} half of our
universal-equational decision procedure: a strong Gr\"obner basis
solver over $\mathbb{Z}_{2^d}$ that decides whether a polynomial
expression equals another \emph{as polynomials} in
$\mathbb{Z}_{2^d}[x_1, \ldots, x_n]$. Section~\ref{sec:vanishing}
gives the function-equivalence half, which extends ideal membership
to the question SMT users actually want answered: are the two
polynomials equal as functions on the bit-vector domain?

We define the underlying algebraic objects
(Section~\ref{sec:algebraic-def}), present the Buchberger-style
computation with our progress-based termination
criterion (Section~\ref{sec:algebraic-algo}), and state our
contributions beyond the framework of
Song et al.~\cite{song2024equational}
(Section~\ref{sec:algebraic-contrib}).

\subsection{Strong Gr\"obner Bases over $\mathbb{Z}_{2^d}$}
\label{sec:algebraic-def}

The ring $\mathbb{Z}_{2^d}$ is not a field: not every non-zero
element has a multiplicative inverse. Only the \emph{units}---the
odd elements, characterised by $\gcd(x, 2^d) = 1$---are invertible.
This non-field structure invalidates the standard Buchberger
algorithm over a field $k$, because Buchberger's S-polynomial
reduction relies on clearing leading coefficients, which requires
division by leading coefficients.

A \emph{strong Gr\"obner basis} generalises the field case to
principal-ideal rings by adding ``coefficient S-polynomials'' that
handle non-unit leading coefficients. Formally, following
Song et al.~\cite{song2024equational}:

\begin{definition}
Let $>$ be a monomial order on $R[x_1, \ldots, x_n]$ where
$R = \mathbb{Z}_{2^d}$, and let $G = \{g_1, \ldots, g_m\}$ be a
finite set of polynomials. For polynomials $p, q$ with leading
terms $\mathrm{lt}(p) = c_p \cdot m_p$ and
$\mathrm{lt}(q) = c_q \cdot m_q$ (where $c_p, c_q \in R$ and
$m_p, m_q$ are monomials), the \emph{S-polynomial}
$S(p, q)$ is
\[
  S(p, q) \;=\; \frac{m}{c_p m_p} \cdot p \;-\;
                \frac{m}{c_q m_q} \cdot q,
\]
where $m = \mathrm{lcm}(m_p, m_q)$ and the scalars must be
handled carefully when $c_p, c_q$ are non-units.
$G$ is a \emph{strong Gr\"obner basis} of the ideal $\langle G \rangle$
if every non-zero $f \in \langle G \rangle$ has its leading term
$\mathrm{lt}(f)$ divisible by some $\mathrm{lt}(g_i)$
\emph{both as monomials and as elements of $R$}.
\end{definition}

The defining property is stronger than the field-case condition
(which requires only monomial divisibility). Song et al.\ show that
strong Gr\"obner bases exist over $\mathbb{Z}_{2^d}$ and can be
computed by an extended Buchberger algorithm.

\paragraph{Uniqueness.}
Over a field, the reduced Gr\"obner basis is unique once the
monomial order is fixed. Over $\mathbb{Z}_{2^d}$ (a principal-ideal
ring with zero divisors) the situation is weaker: the basis is
unique up to multiplication of each generator by a unit, i.e.\ by
an odd element of $\mathbb{Z}_{2^d}$. Two reduced strong Gr\"obner
bases of the same ideal differ at most by such unit-rescalings, and
the membership test (does an ideal element reduce to zero?) is
invariant under them. Empirically, our equation-ordering ablation
(Table~\ref{tab:ordering}) confirms that timings vary within noise
across orderings; the underlying bases are equivalent up to
unit-rescaling and the algorithm's termination behaviour is
unaffected.

\subsection{Buchberger with Progress-Based Termination}
\label{sec:algebraic-algo}

Our implementation extends the standard Buchberger loop with
\emph{progress-based termination}: instead of exhaustively
computing all S-polynomials until saturation (which can be
prohibitively expensive), we terminate early if either
(i)~a unit constant enters the basis (UNSAT detected, lines
13--14 of Algorithm~\ref{alg:buchberger}), or (ii)~the basis has
not changed for $\tau$ steps (pruned early, line 17). In practice
the unit check dominates: on every polynomial-equation benchmark
we have seen, the unit constant appears within $\leq$1,000 steps,
and the global step limit of 100{,}000 is never reached.

\begin{algorithm}[ht]
\SetAlgoLined
\DontPrintSemicolon
\KwIn{polynomial set $F = \{f_1, \ldots, f_k\}$ over $\mathbb{Z}_{2^d}$;
  step limit $L$ (default: 100{,}000)}
\KwOut{UNSAT if unit found; otherwise (inconclusive, reduced basis)}

$G \leftarrow F$; $P \leftarrow \{(f_i, f_j) : i < j\}$;
  $\mathit{steps} \leftarrow 0$\;
\While{$P \neq \emptyset$ \textbf{and} $\mathit{steps} < L$}{
  select pair $(p, q) \in P$ (we use: definitions first, then
    Rabinowitsch equations, then coefficient S-pairs)\;
  $P \leftarrow P \setminus \{(p,q)\}$\;
  $r \leftarrow \mathrm{reduce}(S(p,q), G)$
    \tcp*{S-polynomial reduction}
  $\mathit{steps} \leftarrow \mathit{steps} + 1$\;
  \If{$r = 0$}{\textbf{continue}\;}
  \If{$r$ is a constant $c \in R$ \textbf{and} $c$ is odd}{
    \Return{\textbf{UNSAT}} \tcp*{$c$ is a unit; $\langle G \rangle = R$}
  }
  add pairs $\{(r, g) : g \in G\}$ to $P$\;
  $G \leftarrow G \cup \{r\}$\;
  \If{$G$ unchanged for last $\tau$ iterations}{
    \textbf{break} \tcp*{progress-based termination}
  }
}
\Return{(\textbf{inconclusive}, $G$)}
\caption{Strong Gr\"obner-basis computation with progress-based
  termination}
\label{alg:buchberger}
\end{algorithm}

The pair-selection strategy at line~3 was originally equation-order
sensitive: a naive ordering (Rabinowitsch equations before
definitions) required up to 2{,}000 times more steps than
``definitions before Rabinowitsch'' on benchmarks such as
commutativity and associativity. This 2{,}000$\times$ sensitivity
was eliminated by the progress-based termination rule
(line~17): once all pairs that could contribute to the unit constant
are explored, further pairs cannot help, and the algorithm
terminates regardless of the initial order.

\paragraph{Formalisation-driven refinement.}
We did not discover the progress invariant by tuning the C++
implementation; we discovered it while attempting to prove
termination in Lean~4. Buchberger's standard termination argument
appeals to the ascending chain condition on Noetherian rings, which
guarantees the basis stabilises but says nothing about \emph{when}.
Mechanising the proof forced us to articulate a weaker, operational
property: a step is ``progressive'' if it adds a polynomial whose
leading term is not already divisible by some basis element.
Termination then follows from the finite number of distinct
leading-term ideals, regardless of pair ordering. With this
invariant in hand, we returned to the C++ code and implemented
the early-stop rule: terminate when no progressive step has been
made for $\tau$ iterations. The 2{,}000$\times$ sensitivity that
six months of empirical tuning had not exposed evaporated. We
report this not as a methodological claim but as a concrete
instance of mechanisation paying back into the implementation.

\begin{table}[ht]
\caption{Equation-ordering ablation on the algebraic solver with
  progress-based termination enabled. Four orderings are compared
  on five benchmarks; 5 runs each, mean $\pm$ stddev in ms.
  The ``default'' uses our recommended order (definitions first);
  ``reverse'' activates \texttt{GROEBNER\_REVERSE\_ORDER}; the
  two ``reorder-vars'' variants additionally permute the variable
  order via \texttt{--reorder-vars}. Max/min ratios per row
  fall in $[1.16, 1.26]\times$, compared with the historical
  $2{,}000\times$ sensitivity of the pre-progress-termination
  implementation.}
\label{tab:ordering}
\centering
\small
\begin{tabular}{lrrrr}
\toprule
Benchmark & default & reverse & reorder-vars & reverse+reorder \\
\midrule
comm\_16 & 4.82$\pm$0.38 & 5.44$\pm$0.21 & 4.64$\pm$0.09 & 5.40$\pm$0.12 \\
comm\_32 & 4.68$\pm$0.08 & 5.40$\pm$0.07 & 4.70$\pm$0.00 & 5.46$\pm$0.09 \\
assoc\_8 & 4.66$\pm$0.05 & 5.44$\pm$0.05 & 4.72$\pm$0.28 & 5.42$\pm$0.04 \\
distrib\_8 & 4.68$\pm$0.04 & 5.42$\pm$0.11 & 4.78$\pm$0.08 & 5.42$\pm$0.04 \\
overflow\_detect\_16 & 7.62$\pm$0.04 & 8.22$\pm$0.13 & 7.74$\pm$0.23 & 8.38$\pm$0.19 \\
\bottomrule
\end{tabular}
\end{table}

The current solver's maximum variation across the tested
orderings is $1.26\times$ (see Table~\ref{tab:ordering}). The
residual variation is consistent with measurement noise (standard
deviation within each ordering is comparable to the difference
between orderings), and there is no systematic benefit of any
particular ordering. The $2{,}000\times$ number from early
experiments measured how bad the unordered version could be
without the progress rule, not any sensitivity of the current
implementation.

\subsection{Contributions Beyond Song et al.\ 2024}
\label{sec:algebraic-contrib}

Song et al.~\cite{song2024equational} established that strong
Gr\"obner bases over $\mathbb{Z}_{2^d}$ solve the equational theory
of bit-vectors, and provided a paper-and-pencil proof of this
result. We translate that foundation into a working SMT
component along three axes:

\begin{enumerate}
\item \textbf{End-to-end SMT integration.} The strong Gr\"obner
  basis is wired into \cbmc's post-SSA pipeline so it runs
  transparently on every bit-vector query, with no user
  annotation. SMT queries contain disequalities and deeply nested
  inline multiplications that fall outside the equational theory
  Song et al.\ analyse: we handle disequalities via the
  Rabinowitsch trick (an auxiliary polynomial
  $(c - d) \cdot e - 1 = 0$ that reduces disequation
  satisfiability to unit-constant detection) and decompose nested
  multiplications into fresh variables with defining equations,
  giving Buchberger a flat, uniform polynomial set.

\item \textbf{Algorithmic refinements for SMT-scale problems.}
  Standard Buchberger is sensitive to equation ordering and can
  diverge before reaching a unit constant. We add
  progress-based termination
  (Section~\ref{sec:algebraic-algo}) that eliminates the
  ordering sensitivity, and candidate extraction with
  assumption-gated SAT hints: when the algebraic layer is
  inconclusive (non-unit basis), we extract a partial model and
  pass it as soft hints to the bit-blasting layer, avoiding
  duplicated exploration.

\item \textbf{Formalisation-driven refinement.} 25 theorems in
  Lean 4 with zero \texttt{sorry}, including a Mathlib contribution
  (under review) on the unit characterisation of $\mathbb{Z}_{2^d}$.
  The Lean termination proof exposed an operational invariant
  (Section~\ref{sec:algebraic-algo}) that, when ported back to
  the C++ implementation, eliminated a 2{,}000$\times$ ordering
  sensitivity that empirical tuning had not caught.
\end{enumerate}

The combined procedure solves all 39 benchmarks in our custom
suite, including all polynomial-equation benchmarks that
bit-blasting alone times out on; details in Section~\ref{sec:external}.

\subsection{Solver Pipeline}
\label{sec:algebraic-pipeline}

The solver runs before bit-blasting. Polynomial equations are
collected during SSA conversion: each \texttt{c = a*b} becomes
$c - a \cdot b = 0$,
and negated assertions use the Rabinowitsch trick ($c \neq d$
becomes $(c-d) \cdot e - 1 = 0$). A vanishing polynomial
test~\cite{shekhar2007equivalence} runs first, checking whether the
difference polynomial vanishes as a function over
$\mathbb{Z}_{2^d}$ (complete for polynomial equivalence). If
inconclusive, the Gr\"obner basis handles the full system including
the Rabinowitsch equation. If the Gr\"obner basis contains
a unit constant, the system is unsatisfiable. In $\mathbb{Z}_{2^d}$,
the units are precisely the odd elements (those coprime to $2^d$),
so the check reduces to testing whether any constant in the basis
is odd. When this holds, the system
is unsatisfiable and no bit-blasting is needed. The computation
uses a step limit of 100,000; all polynomial equation benchmarks
converge within 1,000 steps. When the step limit is reached, the
solver returns inconclusive and falls through to bit-blasting; we
have not encountered this on any benchmark in our suite.

\begin{theorem}[Soundness]
If the strong Gr\"obner basis of
$\{f_1, \ldots, f_k, (a{-}b) \cdot e - 1\}$ over
$\mathbb{Z}_{2^d}$ contains an odd constant, then $a = b$ for all
solutions of $\{f_1 = 0, \ldots, f_k = 0\}$.
\end{theorem}

\begin{proof}[Proof sketch]
An odd element of $\mathbb{Z}_{2^d}$ is a unit (coprime to $2^d$).
A unit in an ideal $I$ implies $1 \in I$, hence $I$ is the whole
ring. But if a solution $\sigma$ existed with $f_i(\sigma) = 0$ for
all $i$, the evaluation homomorphism $\phi_\sigma$ would send every
element of $I$ to $0$, contradicting $\phi_\sigma(1) = 1 \neq 0$.
The Rabinowitsch polynomial $(a{-}b) \cdot e - 1$ ensures that any
solution must satisfy $a = b$: if $a \neq b$ then $e = (a{-}b)^{-1}$
would need to exist, but the polynomial $-1$ would be in $I$,
triggering the unit check. The algorithm is sound (never falsely
reports UNSAT) but incomplete (returns inconclusive when the basis
contains only even constants).
Full proof mechanised in Lean~4 (25 theorems, zero \texttt{sorry}),
covering: (a)~unit characterisation in $\mathbb{Z}_{2^d}$ (odd
$\Leftrightarrow$ unit, 2 is not a unit for $d \geq 1$),
(b)~ideal preservation under S-polynomial computation and reduction,
(c)~soundness of the Rabinowitsch trick (unit in ideal $\Rightarrow$
no solution via ring homomorphism), and (d)~Buchberger termination
via the ascending chain condition in Noetherian rings.
\end{proof}

\begin{table}[ht]
\caption{Five-layer arithmetic-solving pipeline in \cbmc with
  representative benchmarks per layer, timed via the
  \texttt{smt2\_solver} interface (which exposes the post-SSA
  pipeline). Numbers in seconds.}
\label{tab:wordlevel}
\centering
\small
\begin{tabular}{lr}
\toprule
Layer / example & smt2 \\
\midrule
\multicolumn{2}{l}{\emph{1. Word-level simplification (CBMC front-end; not exposed via smt2)}} \\
\multicolumn{2}{l}{\quad inline \texttt{a*b == b*a} and \texttt{(a*b)*c == a*(b*c)} fold to \texttt{true} in C source.} \\
\midrule
\multicolumn{2}{l}{\emph{2. Vanishing polynomial test (DSP datapath equivalence)}} \\
\quad dsp\_image\_reject (12$\times$8-bit) & 0.014 \\
\quad dsp\_horner\_16 & 0.009 \\
\quad dsp\_vanishing\_poly\_8 & 0.008 \\
\midrule
\multicolumn{2}{l}{\emph{3. Strong Gr\"obner basis over $\mathbb{Z}_{2^d}$}} \\
\quad commutativity BW=16 & 0.009 \\
\quad commutativity BW=32 & 0.012 \\
\quad associativity BW=8 & 0.009 \\
\quad distributivity BW=8 & 0.006 \\
\quad overflow check BW=16 (extract(15,0) handled as modular reduction) & 0.008 \\
\midrule
\multicolumn{2}{l}{\emph{4. \combacs (bit-blasting, 2 multiplications)}} \\
\quad matrix trace $\mathrm{tr}(AB) = \mathrm{tr}(BA)$ (timed via CBMC C front-end) & 1.8 \\
\quad MAC dot-product commutativity (CBMC C front-end) & 4.0 \\
\midrule
\multicolumn{2}{l}{\emph{5. Shift-add (bit-blasting, 3+ multiplications)}} \\
\quad keyed hash with XOR (CBMC C front-end) & 6.3 \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Layer ablation.}
To quantify the contribution of each layer we disabled layers
sequentially and measured solved counts and running time on 12
representative benchmarks (\cadical, 120\,s T/O). Results in
Table~\ref{tab:ablation} use the same benchmark set and
configurations as in the bit-blasting companion
paper's~\cite{companion-bitblasting} ablation.

\begin{table}[ht]
\caption{Layer ablation (\cadical, seconds; T/O\,=\,120\,s).
  Columns are cumulative: each configuration adds its layer to
  all previous configurations. Bold indicates a new benchmark
  solved by adding that layer.}
\label{tab:ablation}
\centering
\small
\begin{tabular}{lrrrr}
\toprule
Benchmark & shift only & +simplify & +algebraic & +\combacs \\
\midrule
comm\_8 & 0.72 & \textbf{0.005} & 0.005 & 0.005 \\
comm\_16 & T/O & \textbf{0.005} & 0.005 & 0.005 \\
comm\_32 & T/O & \textbf{0.005} & 0.005 & 0.005 \\
assoc\_8 & 29.1 & \textbf{0.005} & 0.005 & 0.005 \\
distrib\_8 & 78.4 & \textbf{0.005} & 0.005 & 0.005 \\
overflow\_detect\_16 & 0.35 & 0.35 & \textbf{0.008} & 0.008 \\
dsp\_vanishing\_poly\_8 & 0.040 & 0.039 & \textbf{0.008} & 0.008 \\
dsp\_horner\_16 & 7.77 & 7.74 & \textbf{0.010} & 0.010 \\
dsp\_image\_reject & T/O & T/O & \textbf{0.015} & 0.015 \\
mul\_ineq\_12 & 0.53 & 0.51 & 0.52 & 0.51 \\
strength\_chain\_16 & 1.07 & 1.07 & 1.08 & 1.50 \\
bf16\_mul\_comm\_v2 & 23.0 & 22.9 & 23.1 & \textbf{11.2} \\
\midrule
Benchmarks solved & 9/12 & 11/12 & 12/12 & 12/12 \\
\bottomrule
\end{tabular}
\end{table}

Three regimes emerge: (i) \emph{word-level simplified}
(comm, assoc, distrib) — the C front-end closes the query at
layer 1 before reaching the solver; (ii) \emph{algebraically
solved} (overflow\_detect, dsp\_vanishing\_poly, dsp\_horner,
dsp\_image\_reject) — layers 2--3 reduce to unit constants or
vanishing polynomials; (iii) \emph{bit-blasting required}
(mul\_ineq\_12 contains a comparison; strength\_chain\_16 is
decomposed by shift; bf16\_mul\_comm\_v2 uses floating-point
semantics) — layers 4--5 handle these. The algebraic layer
(2+3) adds +1 solve (dsp\_image\_reject) and order-of-magnitude
speedups on four benchmarks, while \combacs at layer 4 adds the
final 2$\times$ on bf16 but solves no new instances on this
subset.

The Gr\"obner basis computation is bitwidth-independent: commutativity
at BW=32 takes the same 0.3\,ms as BW=16. For the
\texttt{smt2\_solver} (which lacks word-level simplification), inline
multiplications are decomposed into fresh variables with defining
equations, enabling the Gr\"obner basis to solve associativity
(0.009\,s) and distributivity (0.006\,s) that previously required
26--71\,s of SAT solving. Notably, the Gr\"obner basis makes the
multiplier encoding and SAT solver choice \emph{irrelevant} for
polynomial equation benchmarks: all encoding$\times$solver
combinations produce the same $<$10\,ms result.

When the Gr\"obner basis returns inconclusive (non-polynomial
residual constraints), a candidate assignment is extracted from the
reduced basis and used to generate assumption-gated SAT hints that
guide the bit-blasting solver. Problems with non-polynomial
operations (bitwise XOR, shifts, inequalities) fall through to
bit-blasting with \combacs. The implementation uses \cbmc's existing
\texttt{mp\_integer} for coefficient arithmetic, with no external
dependencies ($\sim$700 lines). The Gr\"obner basis computation is
a standalone decision procedure for the polynomial fragment of
QF\_BV (equations involving addition, multiplication, and
truncation); problems with bitwise operations, inequalities, or
shifts fall through to bit-blasting.

We measured the overhead of unsuccessful algebraic layers on
benchmarks that require SAT solving (bf16\_mul\_comm\_v2,
strength\_chain\_16, div\_mul\_roundtrip\_12). Word-level
simplification adds $<$0.02\,s, and the vanishing polynomial test
plus Gr\"obner basis add $<$0.05\,s---less than 0.5\% of the
hardest benchmark's solving time. The layers are effectively free
when they do not apply.

\subsection{Bitwidth Independence}
\label{sec:scaling}

A distinctive property of the algebraic procedure is that its
running time is essentially independent of the bit-vector width.
We verify this empirically on commutativity and associativity
benchmarks at bitwidths 8 to 256 and compare against
Bitwuzla~\cite{niemetz2023bitwuzla}, the SMT-COMP 2024 QF\_BV
winner, which uses word-level rewriting. We also show \cbmc's
shift-add bit-blasting baseline as a reference for what naive
encoding would do (median of 4 warm runs, 5 runs total with the
first discarded).

\begin{table}[ht]
\caption{Bitwidth scaling on the commutativity and associativity
  family (ms; median of 4 warm runs; 60\,s timeout). Both the
  Gr\"obner-basis solver and Bitwuzla's word-level rewriting are
  bitwidth-independent; \cbmc's shift-add bit-blasting baseline
  times out at BW$=$16 and beyond.}
\label{tab:scaling}
\centering
\begin{tabular}{lrrrrrr}
\toprule
Configuration & BW$=$8 & BW$=$16 & BW$=$32 & BW$=$64 & BW$=$128 & BW$=$256 \\
\midrule
\multicolumn{7}{l}{\emph{commutativity}} \\
\cbmc (algebraic)        & 2.37 & 2.18 & 2.14 & 2.13 & 2.02 & 2.13 \\
Bitwuzla (word-level)    & 1.59 & 1.51 & 1.62 & 1.48 & 1.56 & 1.59 \\
\cbmc (SAT, shift-add)   & 684 & T/O & T/O & T/O & T/O & T/O \\
\midrule
\multicolumn{7}{l}{\emph{associativity}} \\
\cbmc (algebraic)        & 2.11 & 2.06 & 2.05 & 2.07 & 2.07 & 2.09 \\
Bitwuzla (word-level)    & 1.67 & 1.74 & 1.67 & 1.66 & 1.69 & 1.68 \\
\bottomrule
\end{tabular}
\end{table}

The algebraic solver and Bitwuzla both exhibit bitwidth-independent
behaviour, reaching the same answer in 2--3\,ms regardless of
operand width. The mechanisms differ: our procedure recognises the
identity through Gr\"obner-basis reduction of the
Rabinowitsch-augmented polynomial system, while Bitwuzla performs
syntactic word-level rewriting that recognises commutativity and
associativity directly. Both approaches give a structural reason
why the equation holds; both avoid bit-blasting entirely. The
practical implication is that on simple polynomial-equation
queries, mature word-level reasoners already do the right thing,
and our algebraic procedure is most useful when the polynomial
structure is harder to recognise syntactically (Sections~\ref{sec:vanishing},
\ref{sec:eval-random-poly}).

The shift-add bit-blasting baseline is included for reference: it
shows that an SMT solver without word-level reasoning would face
exponential blow-up in bitwidth on these queries. This is a
property of the pure-bit-blasting approach, not of bit-vector
reasoning in general.

The Gr\"obner-basis solver's time is dominated by (a) equation
collection, (b) polynomial normalisation over $\mathbb{Z}_{2^d}$,
and (c) a small number of S-polynomial reductions until the unit
constant appears. None of these scales with $d$: coefficient
arithmetic uses arbitrary-precision integers (via \cbmc's
\texttt{mp\_integer}), and the polynomial structure is determined
by the problem shape, not the bitwidth.


\section{Vanishing Polynomial Test}
\label{sec:vanishing}

Ideal membership in $\mathbb{Z}_{2^d}[x_1, \ldots, x_n]$ is not the
question an SMT user is asking. A verification engineer querying
$\forall a, b. \, f(a, b) = g(a, b)$ over 16-bit integers means
``for all 16-bit values of $a$ and $b$'' --- they want
\emph{function equivalence} on the bit-vector domain. Over a field,
the two coincide; over $\mathbb{Z}_{2^d}$ they do not. Some
polynomials are non-zero in $\mathbb{Z}_{2^d}[x]$ but vanish as
functions on $\mathbb{Z}_{2^d}$: the canonical example is
$4x^2 + 4x \equiv 0 \pmod{8}$ for all
$x \in \{0, 1, \ldots, 7\}$, even though the polynomial has
non-zero coefficients. We refer to such polynomials as
\emph{vanishing polynomials}~\cite{shekhar2007equivalence}; they
exist because $\mathbb{Z}_{2^d}[x]$ has more polynomials than
there are distinct $\mathbb{Z}_{2^d}$-valued functions on
$\mathbb{Z}_{2^d}$.

The Gr\"obner-basis-only treatment of
Song et al.~\cite{song2024equational} decides the equational
theory (polynomial identity in $\mathbb{Z}_{2^d}[x_1, \ldots,
x_n]$), not the functional theory (equivalence as functions on
$\mathbb{Z}_{2^d}^n \to \mathbb{Z}_{2^d}$). For SMT queries the
gap matters: it surfaces in DSP datapath verification where
coefficient scaling causes overflow cancellation, and the two
sides of an equivalence query differ as polynomials but agree as
functions.

This section gives a decision procedure for function equivalence
over $\mathbb{Z}_{2^{n_1}} \times \cdots \times
\mathbb{Z}_{2^{n_k}} \to \mathbb{Z}_{2^m}$, complete in that
sense. We adapt the algebraic test of
Shekhar et al.~\cite{shekhar2007equivalence}, with the efficient
falling-factorial / Stirling-number / Kronecker-product
normalisation algorithm of
G\`amez-Montolio et al.~\cite{gamez2024efficient}.

\paragraph{Why a separate test, instead of adding vanishing
polynomials to the Gr\"obner basis?}
A natural alternative is to add the generators of the vanishing
ideal --- the polynomials
$2^{\max(d - \nu_2(k!),\ 0)} \cdot x^{\underline{k}}$ for
$k = 2, \ldots, \mathrm{SF}(2^d)$, per input variable --- directly
to the input basis of Buchberger's algorithm. The resulting strong
Gr\"obner basis would automatically reduce vanishing polynomials to
zero, so ideal membership would coincide with functional
equivalence and a separate test would be unnecessary.

We implemented this experimentally and found it impractical. On
the full 39-benchmark custom suite, replacing the falling-factorial
test with vanishing-polynomial injection \emph{loses} 9 of 39
benchmarks --- including 7
(\texttt{overflow\_detect\_16}, \texttt{add\_overflow\_16},
\texttt{checked\_mul\_16}, \texttt{equiv\_unsat\_8add\_16},
\texttt{crypto\_square\_mod}, \texttt{mul\_ineq\_12},
\texttt{fixedpoint\_mul\_16}) that do not need vanishing reasoning
at all and solve trivially without the test. Adding the injection
on \emph{top} of the working vanishing test causes 2 further
regressions
(\texttt{div\_mul\_roundtrip\_12}: $3.97\,$s $\to$ T/O,
\texttt{fixedpoint\_mul\_16}: $0.02\,$s $\to$ T/O). Two failure modes:
(i) For $d = 8$, $\mathrm{SF}(2^d) = 10$, so 9 generators per
variable are added; their pairwise S-polynomials bloat
Buchberger's worklist even when not needed, hitting the
100\,000-step reduction budget.
(ii) The DSP benchmarks introduce intermediate variables for
nested multiplications via SSA (\texttt{x2 = x*x}, \texttt{x3 =
x*x2}, $\ldots$, up to \texttt{x10}). To use a degree-10 vanishing
polynomial in $x$, Buchberger must first reduce the polynomial
back to a polynomial in $x$ alone, an S-polynomial chain that
exceeds the step budget. The falling-factorial test sidesteps
both issues by substituting SSA definitions before testing and
converting directly to the factorial basis. Full data in the
supplementary material.

\paragraph{Direction of completeness.}
The vanishing-polynomial test, and the Gr\"obner-basis solver
of Section~\ref{sec:algebraic} more broadly, decide UNSAT-direction
queries: they prove that two polynomials are equal as functions
(equivalently, that a disequality is unsatisfiable) but cannot
construct a witness when they differ. SAT-finding queries
(``does $p(x) = c$ have a solution?'', root-finding, factoring)
require the bit-blast layer instead. Section~\ref{sec:eval-random-poly}
quantifies this: on random polynomial-identity benchmarks our
algebraic procedure dominates on UNSAT-oriented categories
(addition / multiplication / encoding-correctness identities) but
loses to bit-blasting on equality and nonequality SAT-finding
queries.

\subsection{Falling Factorials and Stirling Numbers}
\label{sec:vanishing-falling}

Let $R = \mathbb{Z}_{2^m}$ and consider $R$-valued functions on
$k$ bit-vector inputs of widths $n_1, \ldots, n_k$. For a natural
number $j$, the \emph{falling factorial} is
\[
  x^{\underline{j}} \;=\; x \cdot (x{-}1) \cdot (x{-}2) \cdots (x{-}j{+}1),
\]
with $x^{\underline{0}} = 1$. The polynomials $\{x^{\underline{j}}\}_{j \geq 0}$
form a basis of $R[x]$ alternative to the monomial basis
$\{x^j\}_{j \geq 0}$.

\emph{Stirling numbers of the second kind}, $S(n,k)$, encode the
change of basis from monomials to falling factorials:
\[
  x^n \;=\; \sum_{k=0}^{n} S(n,k) \, x^{\underline{k}},
  \qquad S(n,k) \;=\; \frac{1}{k!} \sum_{j=0}^{k} (-1)^{k-j}
                       \binom{k}{j} j^{\,n}.
\]
Combinatorially, $S(n,k)$ counts the number of ways to partition
an $n$-element set into $k$ non-empty blocks. For bit-vector
verification, the key property is that in $R = \mathbb{Z}_{2^m}$,
the falling factorial $x^{\underline{j}}$ is divisible by $j!$ as a
function of $x$ (since any consecutive $j$ integers contain a
multiple of each prime power up to the $j!$ factor). Hence the
function $x^{\underline{j}} \bmod 2^m$ is identically zero whenever
$\nu_2(j!) \geq m$, where $\nu_2$ is the 2-adic valuation.

\begin{theorem}[Vanishing criterion~\cite{shekhar2007equivalence}]
\label{thm:vanishing}
A univariate polynomial $p(x) = \sum_j c_j \, x^{\underline{j}} \in R[x]$
vanishes as a function on $\mathbb{Z}_{2^n}$ if and only if for each
$j$,
\[
  2^{\,m - \min(\nu_2(j!),\,m)} \;\big|\; c_j \quad\text{in }\mathbb{Z}.
\]
\end{theorem}

\subsection{Multivariate Extension via Kronecker Product}
\label{sec:vanishing-kron}

For a multivariate polynomial $p(x_1, \ldots, x_k) \in R[x_1, \ldots, x_k]$
we apply the change of basis independently per variable. Let
$S_{n_i}$ denote the $n_i \times n_i$ Stirling matrix of the second
kind. The change-of-basis matrix for the tensor product
$R[x_1] \otimes \cdots \otimes R[x_k]$ is the Kronecker product
$S_{n_1} \otimes \cdots \otimes S_{n_k}$, mapping the coefficient
vector in the monomial basis to the coefficient vector in the
multivariate falling-factorial basis $\{x_1^{\underline{j_1}} \cdots
x_k^{\underline{j_k}}\}$.

The vanishing criterion extends: $p$ is identically zero as a
function iff every multivariate falling-factorial coefficient
$c_{j_1, \ldots, j_k}$ satisfies the divisibility
$2^{m - \min(\nu_2(\prod_i j_i!), m)} \mid c_{j_1, \ldots, j_k}$.

\paragraph{Mixed input widths.}
Variables in the same polynomial ring $R[x_1, \ldots, x_k]$ may have
different effective input widths $n_i \leq d$ (e.g., when narrower
operands are zero-extended into a wider product). The criterion
above is incomplete for that setting: the falling factorial
$x_i^{\underline{k}}$ is identically zero on
$\{0, 1, \ldots, 2^{n_i} - 1\}$ once $k \geq 2^{n_i}$, so terms
exceeding that bound vanish on the input range without contributing
a divisibility constraint. Our implementation tracks the input width
of each variable through the polynomial extractor and adjusts the
per-variable degree bound to $\mu_i = \min(2^{n_i}, \mathrm{SF}(2^d))$.
Two of our DSP benchmarks exercise this mixed-width form:
\texttt{dsp\_image\_reject}
($\mathbb{Z}_{2^{12}} \times \mathbb{Z}_{2^8} \to \mathbb{Z}_{2^{16}}$,
solved in 14\,ms) and \texttt{dsp\_vanishing\_mv}
($\mathbb{Z}_2 \times \mathbb{Z}_{2^2} \to \mathbb{Z}_{2^3}$,
solved in 5\,ms).

\subsection{Algorithm and Complexity}

\begin{enumerate}
\item Convert the difference polynomial to the falling factorial
  basis using Stirling numbers of the second kind $S(n,k)$
  (Section~\ref{sec:vanishing-falling}).
\item For multivariate polynomials, use the Kronecker product
  $S^{\otimes k}$ for the basis change (Section~\ref{sec:vanishing-kron}).
\item Check the divisibility conditions of Theorem~\ref{thm:vanishing}.
\item If all conditions hold, the polynomial vanishes.
\end{enumerate}

\textbf{Complexity derivation.} Let $d_w$ denote the maximum
per-variable degree of the difference polynomial (in the monomial
basis), and $k$ the number of variables. The monomial basis has
$d_w^k$ elements; the Kronecker Stirling matrix has
$d_w^k \times d_w^k$ entries. The basis change is a single
matrix-vector product, giving cost $O(d_w^{2k})$. For the typical
DSP datapaths we verify, $d_w \leq 4$ and $k \leq 4$, so the matrix
has at most $4^4 = 256$ rows; the test completes in microseconds.
For higher-degree benchmarks the exponential dependence on $k$ is
real but bounded in practice by the polynomial structure of DSP
filters (which rarely exceed degree 3 per variable and 4 variables).
The test is complete for polynomial equivalence over
$\mathbb{Z}_{2^{n_1}} \times \cdots \times \mathbb{Z}_{2^{n_d}}
\to \mathbb{Z}_{2^m}$.

\paragraph{Empirical scaling.}
Figure~\ref{fig:scaling} reports two scaling experiments: degree
scaling on a family of binomial identities $(a + b)^k =
\sum_{i=0}^k \binom{k}{i} a^i b^{k-i}$ for
$k \in \{2,\ldots,6\}$ at bitwidths 8 and 16, and variables
scaling on a permutation family
$\sum_{\sigma \in S_k} \prod_i x_{\sigma(i)} \equiv
k! \cdot \prod_i x_i \pmod{2^{16}}$ for $k \in \{2,\ldots,6\}$ (the
LHS contains $k \cdot k!$ multiplications). All measurements use
median of 4 warm runs (5 runs total, first discarded), consistent
with Section~\ref{sec:scaling}.

\begin{figure}[ht]
  \centering
  \includegraphics[width=0.95\textwidth]{scaling-figure.pdf}
  \caption{Algebraic-solver scaling. (a) On the binomial-identity
    family $(a+b)^k$, wall time grows by $2.1\times$ at BW=8 and
    $3.1\times$ at BW=16 as $k$ goes from 2 to 6 --- well below
    the theoretical $O(d_w^{2k})$ bound, since fixed overhead
    (SMT parsing, SSA construction, Buchberger setup) dominates
    these short solves. (b) On the permutation family
    $\sum_{\sigma \in S_k} \prod_i x_{\sigma(i)}$, wall time grows
    as approximately $k^5$ (log--log slope 4.9), driven by the
    $k \cdot k!$ multiplications on the LHS. The Gr\"obner basis
    recognises equivalent monomials via commutativity in the
    ideal, rather than enumerating all $k!$ permutations
    separately; growth is sub-factorial against $k \log k$
    (slope 0.6 versus 1.0 for factorial). At $k=6$ the 2.2\,s is
    dominated by the 720 reductions required to unify the
    permutation terms, not by polynomial arithmetic itself.}
  \label{fig:scaling}
\end{figure}

\subsection{Worked Example: $4x^2 + 4x \equiv 0 \pmod{8}$}
\label{sec:vanishing-example}

To see the mechanism, take $R = \mathbb{Z}_8$, $m = 3$, and
$p(x) = 4x^2 + 4x$ as a univariate polynomial.
Convert to falling factorials using $x^2 = x^{\underline{2}} + x^{\underline{1}}$
and $x = x^{\underline{1}}$:
\[
  p(x) \;=\; 4(x^{\underline{2}} + x^{\underline{1}}) + 4 x^{\underline{1}}
        \;=\; 4 \, x^{\underline{2}} + 8 \, x^{\underline{1}}.
\]
Apply Theorem~\ref{thm:vanishing}. For $c_2 = 4$: $\nu_2(2!) = 1$,
so we need $2^{3-1} = 4 \mid 4$, which holds. For $c_1 = 8$:
$\nu_2(1!) = 0$, so we need $2^3 = 8 \mid 8$, which also holds.
All conditions hold, so $p$ vanishes as a function---which matches
the direct verification $4 \cdot 0 + 4 \cdot 0 = 0$, $4 \cdot 1 + 4 \cdot 1 = 8 \equiv 0$,
$4 \cdot 4 + 4 \cdot 2 = 24 \equiv 0$, and so on.

In the DSP context this is important because a Horner-form filter
and its expanded form typically differ by terms of this shape
(low-order bits cancel through overflow).

\subsection{How Overflow Detection Becomes a Polynomial Query}
\label{sec:overflow-encoding}

Readers may wonder how overflow-detection benchmarks become amenable
to polynomial reasoning, since ``$x$ overflows $N$ bits'' is
quintessentially a non-polynomial bit-level predicate. The mechanism
is structural: overflow-detection benchmarks typically assert a
\emph{disequality} between two values that, modulo $2^N$, are
necessarily equal --- so the disequality reduces to a polynomial
identity in $\mathbb{Z}_{2^N}$ regardless of whether overflow occurs.

Consider \texttt{overflow\_detect\_16}:
\begin{verbatim}
(define-fun wide_prod   () (_ BitVec 32) (bvmul wa wb))
(define-fun narrow_prod () (_ BitVec 16) (bvmul a b))
(assert (not (= narrow_prod ((_ extract 15 0) wide_prod))))
\end{verbatim}
The assertion is unsatisfiable not because of any reasoning about
the high 16 bits of \texttt{wide\_prod}, but because
$\texttt{narrow\_prod} = a\cdot b \bmod 2^{16}$ and
$\texttt{extract}(15,0)(\texttt{wide\_prod})
= (\texttt{zero\_extend}(a) \cdot \texttt{zero\_extend}(b)) \bmod 2^{16}
= a\cdot b \bmod 2^{16}$. The Gr\"obner basis recognises the polynomial
identity in $\mathbb{Z}_{2^{16}}$. The ``overflow'' part of the query
acts as an unconstrained assumption that the algebraic layer accepts
without analysing.

This is the typical encoding: an overflow predicate is paired with a
disequality whose two sides reduce to the same polynomial in the
narrow ring. Where an overflow predicate appears as the \emph{primary}
constraint --- e.g.\ ``prove that $a\cdot b$ overflows iff
$a > 2^N / b$'' --- our extractor cannot turn the high-bit extraction
\texttt{(extract $2N{-}1$ $N$)} into a polynomial directly (the
non-zero low index is a shift, which is non-polynomial); the query
falls through to the bit-blast layer. Such cases (e.g.\ the
no-overflow assumptions in \texttt{mul\_ineq\_12}) still benefit
indirectly: the Gr\"obner basis simplifies the multiplication-related
sub-queries, leaving the bit-blast layer with a smaller search.

\subsection{Worked Example: Gr\"obner Basis for Commutativity at BW=3}
\label{sec:grobner-example}

Consider $a \cdot b = b \cdot a$ over $\mathbb{Z}_8$ ($d = 3$).
The Rabinowitsch-augmented polynomial system is
\[
  \{\; p_1 = z_1 - a \cdot b, \quad
       p_2 = z_2 - b \cdot a, \quad
       p_3 = (z_1 - z_2) \cdot e - 1 \;\},
\]
with lex order $e > z_1 > z_2 > a > b$.

Step 1. Reduce $p_3$ by $p_1$: substitute $z_1 \mapsto a \cdot b$
(modulo the sign of the S-polynomial), giving
$p_3' = (a \cdot b - z_2) \cdot e - 1$.

Step 2. Reduce $p_3'$ by $p_2$: substitute $z_2 \mapsto b \cdot a$,
giving $p_3'' = (a \cdot b - b \cdot a) \cdot e - 1$.

Step 3. In $\mathbb{Z}_8[a, b, e]$, $a \cdot b = b \cdot a$ holds as
a polynomial identity (commutativity of the ring), so
$p_3'' = 0 \cdot e - 1 = -1$. The constant $-1 \equiv 7 \pmod{8}$ is
odd, hence a unit: the Gr\"obner basis contains a unit, so the
original system is unsatisfiable, proving $z_1 = z_2$ for all
assignments. This takes three reductions, regardless of $d$. In
our implementation the cost is ${\sim}$200\,$\mu$s independent of
bitwidth.

\subsection{Future Directions Within and Beyond the Universal-Equational Class}
\label{sec:future-within-and-beyond}

The procedure of Sections~\ref{sec:algebraic} and~\ref{sec:vanishing}
treats the algebraic layer as a preprocessor: it runs once,
decides what it can on the universal-equational class, and the
residual goes to bit-blasting. Two extensions naturally arise.

\paragraph{Within the universal-equational class: expression-level
normalisation.}
After Buchberger, reduce arbitrary polynomial expressions in the
residual w.r.t.\ the computed basis. If the reduction produces a
constant or a shorter polynomial form, communicate it to the
bit-blast layer as a SAT-level equality, propagating the algebraic
insight. This generalises our existing \texttt{extract\_candidate}
(which finds univariate-linear basis elements only) to arbitrary
expressions, making the algebraic layer a true theory solver in
the SMT(LRA/UF/$\ldots$)\ tradition --- exchanging information
bidirectionally with the SAT search rather than running once and
stopping. The class boundary is unchanged; only the depth of
interaction within it deepens.

\paragraph{Toward the relational classes: bit-decomposition
variables.}
Our procedure does not address the universal-relational and
existential-relational classes (Table~\ref{tab:taxonomy}); these
are the bit-blasting companion paper's domain. Bit-decomposition
gives a route to extend the algebraic layer's coverage:
SAT-level partial assignments often determine specific bits of
intermediate values. Encoding these via fresh bit-decomposition
variables $b_{x,i}$ over $\mathbb{Z}_{2^d}$, with idempotency
$b_{x,i}^2 = b_{x,i}$ and the sum-decomposition
$x = \sum_i 2^i b_{x,i}$, allows the algebraic layer to
incorporate bit-level facts and to discover bit-level
consequences. Predicates like ``high bit of $x \cdot y$ is 1''
become polynomial constraints, bringing relational queries
(bounds, overflow witnesses) into reach of the same Gr\"obner
infrastructure. This is the direction in which a future revision
takes the algebraic procedure beyond its current
universal-equational scope.

\section{Evaluation}
\label{sec:external}

We evaluate on five benchmark pools, organised by the role each
plays in the contribution narrative. \emph{Pools where the
algebraic procedure decides queries no current solver decides:}
the 210-benchmark random-polynomial sample of
Brain~\cite{brain2025subpoly} (Section~\ref{sec:eval-random-poly}),
which contributes the four wins beyond Bitwuzla, cvc5, and every
bit-blasting configuration we tested; and 5 DSP datapath
equivalences (Section~\ref{sec:eval-dsp}) where overflow
cancellation makes the vanishing-polynomial test necessary.
\emph{Pool where the procedure validates correctness and bit-vector
coverage:} the 39-benchmark custom suite
(Section~\ref{sec:eval-custom}), which exercises the algebraic and
vanishing layers across five categories. \emph{Pool where the
procedure is a complement, not a replacement:} the 66-benchmark
SMT-COMP 2024 stratified sample
(Section~\ref{sec:eval-smtcomp}),
where Bitwuzla's mature word-level rewriting wins the volume
contest. \emph{Scope comparison:} the Amulet2 gate-level
verifier (Section~\ref{sec:eval-amulet}). Threats to validity
are consolidated in Section~\ref{sec:threats}.

The baseline solvers are Bitwuzla~\cite{niemetz2023bitwuzla}
(SMT-COMP 2024 QF\_BV winner, version 0.9.0-dev) and cvc5
(version 1.3.3); neither implements a Gr\"obner-basis decision
procedure for QF\_BV (cvc5's Gr\"obner-basis reasoning is in its
finite-field theory and applies only to QF\_FF queries over
prime-order fields, not QF\_BV). All experiments run under a
60\,s wall-clock limit (matching SMT-COMP's short track) and a
6\,GB memory limit, except the random-polynomial suite which uses
10\,s to match the prior comparison.

\subsection{Random Polynomial Identity Suite (210 benchmarks)}
\label{sec:eval-random-poly}

We evaluate on a stratified sample of 210 benchmarks from the
random polynomial benchmark suite of
Brain~\cite{brain2025subpoly}, drawn across two random seeds and
eight categories. The benchmarks test arithmetic identities at
random polynomial degrees 2--53 over bitwidths 8, 16, 32, 64.
Categories cover universal-equational identities (addition,
multiplication, encoding-correctness), existential-equational
SAT-direction queries (equality, nonequality), and bounded /
identity-point / root-finding queries. Five configurations
were measured under a 10\,s wall-clock budget per benchmark:
two pure-bit-blasting baselines (shift-add, Comba/CS), the
algebraic procedure of this work (Comba/CS plus the algebraic
layer), Bitwuzla 0.9.0-dev, and cvc5 1.3.3.

\begin{table}[ht]
\caption{Solved counts on the 210-benchmark random-polynomial
sample, 10\,s wall-clock budget per benchmark.}
\label{tab:random-poly-solved}
\centering
\begin{tabular}{lr}
\toprule
Configuration                       & Solved / 210 \\
\midrule
shift-add bit-blasting              & 118 \\
Comba/CS bit-blasting               & 118 \\
\textbf{this paper} (algebraic)     & \textbf{128} \\
Bitwuzla 0.9.0-dev                  & 184 \\
cvc5 1.3.3                          & 168 \\
\midrule
union (any configuration)           & 188 \\
\bottomrule
\end{tabular}
\end{table}

Bitwuzla wins the volume contest by 56 over our procedure: its
occurrence-map canonicalisation closes most universal-equational
queries syntactically, and on the existential-equational
SAT-direction queries (equality, nonequality) it constructs
witnesses that our UNSAT-oriented Rabinowitsch encoding cannot.
The contribution we claim, however, is not volume but
\emph{completeness}: the algebraic procedure decides 4 benchmarks
that no current SMT solver and no bit-blasting configuration
solves within 10\,s.

\paragraph{Wins beyond all current solvers.}
\begin{table}[ht]
\caption{Four random-polynomial benchmarks decided by our
procedure that Bitwuzla, cvc5, and every bit-blasting
configuration time out on within 10\,s. Times in seconds.}
\label{tab:random-poly-wins}
\centering
\small
\begin{tabular}{lr}
\toprule
Benchmark (seed-23) & this paper \\
\midrule
\texttt{bw-16-deg-13-multiplication-reduced-native-encoding}    & 0.30 \\
\texttt{bw-16-deg-23-correctness-original-vs-reduced-native-encoding} & 0.41 \\
\texttt{bw-32-deg-19-multiplication-reduced-native-encoding}    & 3.73 \\
\texttt{bw-32-deg-27-correctness-original-vs-reduced-native-encoding} & 2.87 \\
\bottomrule
\end{tabular}
\end{table}

The four wins are universal-equational identities at degree
13--27, two at BW=16 and two at BW=32. The Gr\"obner-basis
solver dispatches them in 0.3--3.7\,s; bit-blasting cannot
encode the multiplications, and Bitwuzla's / cvc5's word-level
rewriting does not have rules for the relevant high-degree
expansions.

A weaker but still meaningful claim is the gap to bit-blasting
alone: 18 benchmarks are decided by our procedure that no
bit-blasting configuration we tested solves within 10\,s,
including the four above plus 14 that Bitwuzla also solves via
word-level rewriting. The 18-vs-bit-blasting and 4-vs-everyone
numbers measure two different things: the former reflects what
algebraic reasoning offers over bit-blasting alone, the latter
measures contributions beyond \emph{all} current SMT
infrastructure. We report both because the SMT user has both
options available.

\paragraph{Per-category breakdown.}
The win pattern is structured by the four-classes taxonomy
(Table~\ref{tab:taxonomy}). Algebraic reasoning dominates on
universal-equational categories (addition, multiplication,
correctness identities) where the Rabinowitsch trick reduces
the question to unit-constant detection; it is dominated on
existential-equational SAT-direction (equality, nonequality)
where witness construction is the goal; on the
non-strictly-equational categories (bounding, identity-point,
root-finding) it ties pure bit-blasting because the algebraic
layer falls through.

\begin{table}[ht]
\caption{Per-category solved counts on the random-polynomial
sample. The taxonomy class predicts the win pattern: the
algebraic procedure beats pure bit-blasting on the
universal-equational categories, ties on the
non-strictly-equational categories, and loses on the
existential-equational SAT-direction categories.}
\label{tab:random-poly-by-category}
\centering
\small
\begin{tabular}{llrrrrr}
\toprule
Class & Category & shift-add & Comba/CS & this paper & Bitwuzla & cvc5 \\
\midrule
\multicolumn{7}{l}{\emph{Universal-equational (algebraic dominates pure bit-blasting)}} \\
& addition       & 4/20  & 4/20  & \textbf{11/20} & 17/20 & 17/20 \\
& multiplication & 3/20  & 3/20  & \textbf{11/20} & 14/20 & 8/20 \\
& correctness    & 8/30  & 8/30  & \textbf{11/30} & 14/30 & 12/30 \\
\midrule
\multicolumn{7}{l}{\emph{Existential-equational SAT-direction (bit-blasting dominates)}} \\
& equality       & \textbf{11/20} & \textbf{11/20} & \textbf{11/20} & 20/20 & 19/20 \\
& nonequality    & \textbf{14/20} & \textbf{14/20} & 6/20  & 20/20 & 19/20 \\
\midrule
\multicolumn{7}{l}{\emph{Non-strictly-equational (algebraic ties bit-blasting)}} \\
& bounding       & 16/20 & 16/20 & 16/20 & 20/20 & 20/20 \\
& identity-point & 30/40 & 30/40 & 30/40 & 40/40 & 37/40 \\
& root-finding   & 31/40 & 31/40 & 31/40 & 38/40 & 35/40 \\
\bottomrule
\end{tabular}
\end{table}

We sanity-checked correctness on three benchmarks (one
\texttt{sat}, one \texttt{unsat}, one with mixed expected
behaviour) by running all five configurations and confirming
three-way agreement; we observed no \texttt{sat}-vs-\texttt{unsat}
disagreements between our procedure and the external solvers
across the 121 benchmarks both Bitwuzla and our procedure
solved within budget.

\subsection{DSP Datapath Equivalence (5 benchmarks)}
\label{sec:eval-dsp}

DSP datapath equivalences exhibit overflow cancellation:
two arithmetically distinct polynomials become functionally
equivalent because their difference is a vanishing polynomial
under modular overflow. The vanishing polynomial test of
Section~\ref{sec:vanishing} decides this fragment in
microseconds.

\begin{table}[ht]
\caption{DSP datapath benchmarks (\cadical, seconds, 60\,s
T/O). All three tools solve all five; cvc5 takes 1.84\,s on
\texttt{dsp\_horner\_16} where our vanishing-polynomial test
takes 10\,ms (a 184$\times$ gap). The five benchmarks were
designed by us to exercise the test (see
Section~\ref{sec:threats}).}
\label{tab:dsp}
\centering
\begin{tabular}{lrrr}
\toprule
Benchmark & this paper & Bitwuzla & cvc5 \\
\midrule
\texttt{dsp\_image\_reject} ($\mathbb{Z}_{2^{12}} \times \mathbb{Z}_{2^8} \to \mathbb{Z}_{2^{16}}$) & 0.014 & 0.007 & 0.010 \\
\texttt{dsp\_horner\_16} (Horner vs.\ direct) & 0.010 & 0.004 & \textbf{1.822} \\
\texttt{dsp\_vanishing\_poly\_8} ($Y_{10}(x) \equiv 0$)       & 0.008 & 0.013 & 0.048 \\
\texttt{dsp\_vanishing\_mv} (multivariate)                    & 0.005 & 0.004 & 0.005 \\
\texttt{dsp\_coeff\_scale\_8}                                  & 0.005 & 0.002 & 0.006 \\
\bottomrule
\end{tabular}
\end{table}

The five benchmarks all solve in $<$15\,ms via our procedure,
with the vanishing-polynomial test contributing the decisive
step on \texttt{dsp\_horner\_16} (a Horner-form vs.\ expanded-form
equivalence: arithmetically distinct polynomials, equivalent
modulo $2^{16}$). Bitwuzla solves the same set in similar time
via word-level normalisation; cvc5 falls through to bit-blasting
on \texttt{dsp\_horner\_16}, where it takes 1.84\,s.

The five DSP benchmarks were designed by us, inspired by
Shekhar et al.~\cite{shekhar2007equivalence}; their inclusion
demonstrates the vanishing-polynomial test in operation rather
than independent superiority. The procedure's value is that the
test is \emph{a complete decision procedure for this fragment};
the absolute timings on these particular five benchmarks would
not differ markedly under a future word-level rewriter that
added the appropriate rules.

\subsection{SABER Polynomial Multiplication (Post-Quantum Cryptography)}
\label{sec:eval-saber}

SABER~\cite{daemen2018saber} is a lattice-based key encapsulation
mechanism (NIST PQC Round 3 finalist). Its inner operation is
polynomial multiplication in the ring $R_q = \mathbb{Z}_q[x] / (x^N + 1)$
with $q = 2^{13}$ and $N = 256$ in the standard parameter set,
stored in 16-bit words. SABER's reference
implementation~\cite{KULeuvenSABER} uses Toom-Cook 4-way
decomposition with Karatsuba as the inner kernel, then reduces
the $2N$-coefficient product modulo $x^N + 1$ via the
SABER-specific reduction step
\texttt{res[i] = c[i] - c[i+N]}.

We verify the equivalence of two implementations of the
polynomial product, mirroring SABER's algorithmic structure at
scaled-down $N$ and at the implementation bitwidth $q = 2^{16}$:

\begin{itemize}
\item \emph{Schoolbook}: direct $O(N^2)$ multiplication.
\item \emph{Karatsuba (1-level)}: split inputs into halves; 3
  schoolbook half-products combined via the Karatsuba
  identity.
\item \emph{Karatsuba (2-level)}: split inputs into halves and
  then quarters; 9 schoolbook quarter-products. Mirrors SABER's
  multi-level structural decomposition.
\end{itemize}

Each query asserts the equivalence of all $N$ output coefficients
in a single disjunctive disequality
$\bigvee_{i=0}^{N-1} (\text{A\_res}_i \neq \text{B\_res}_i)$.
Our pre-solver pattern-matches this shape and refutes each
branch via the per-disequality machinery: the disjunction is
unsatisfiable iff every branch is, and each branch is decided
by the vanishing-polynomial test (the diff polynomial is
literally zero after SSA inlining). Per-coefficient queries are
also supported via the \texttt{--single-coeff} flag of the
generator, for fine-grained reproducibility.

\begin{table}[ht]
\caption{SABER polynomial-multiplication equivalence at scaled
$N$ and SABER's storage bitwidth $q = 2^{16}$. Per output
coefficient via the all-coefficients disjunction; 600\,s
timeout. The 1-level / 2-level columns differ only in inner
Karatsuba recursion; both verify the same algebraic identity.
$N = 256$ is SABER's actual parameter. cvc5 times out
($\geq 60\,$s) at every $N \geq 4$.}
\label{tab:saber-scaling}
\centering
\small
\begin{tabular}{lrrrr}
\toprule
$N$ & this paper (1-level) & this paper (2-level) & Bitwuzla & cvc5 \\
\midrule
4   & 0.00\,s   & 0.00\,s   & 0.00\,s & T/O \\
8   & 0.00\,s   & 0.00\,s   & 0.00\,s & T/O \\
16  & 0.01\,s   & 0.01\,s   & 0.01\,s & T/O \\
32  & 0.06\,s   & 0.06\,s   & 0.03\,s & T/O \\
64  & 0.31\,s   & 0.34\,s   & 0.13\,s & T/O \\
128 & 2.00\,s   & 2.05\,s   & 0.50\,s & T/O \\
256 & 14.27\,s  & 14.01\,s  & 2.18\,s & T/O \\
320 & 27.18\,s  & 26.51\,s  & 3.54\,s & T/O \\
512 & 107.98\,s & 104.54\,s & 9.42\,s & T/O \\
768 & 361.35\,s & 346.85\,s & 21.76\,s & T/O \\
\bottomrule
\end{tabular}
\end{table}

The procedure scales to SABER's actual $N = 256$ parameter,
deciding each output-coefficient equivalence in $\sim$14\,s
regardless of whether the alternative implementation is 1-level
or 2-level Karatsuba: Buchberger normalises through the
recursion levels because the underlying polynomial identity is
the same. Bitwuzla solves each in 2.18\,s by occurrence-map
canonicalisation, since both schoolbook and Karatsuba expand
to the same canonical sum-of-monomials form. cvc5 times out
at every benchmark in this family at every $N \geq 4$ --- its
QF\_BV pipeline does not perform the polynomial-distributivity
expansion that Bitwuzla's occurrence-map canonicaliser does.

The win pattern matches the random-polynomial result of
Section~\ref{sec:eval-random-poly}: completeness on the
universal-equational class with polynomial expressions in
$\mathbb{Z}_{2^d}$ is a procedural guarantee, while heuristic
word-level rewriting wins or loses depending on whether the
rewriter has matching patterns. Our procedure decides where
cvc5 cannot. We extend an order of magnitude beyond SABER's
$N = 256$ (to $N = 768$, $\sim$5.8 minutes), demonstrating that
the procedure scales to crypto parameter sizes and to a regime
where bit-blasting alone is no longer competitive: at $N = 512$
the underlying SAT problem has tens of millions of clauses if
emitted, but our deferred-bit-blasting strategy
(Section~\ref{sec:eval-saber-defer}) prevents that emission
when algebraic refutation succeeds.

\paragraph{Toom-Cook 4-way: an open-challenge target.}
SABER's actual implementation uses Toom-Cook 4-way at the
outermost level with interpolation steps that include $\gg 1$
and $\gg 3$ shifts and modular inverses of $3$, $9$, $15$.
With the bit-decomposition extension of
Section~\ref{sec:eval-pbv}, our extractor can now produce a
faithful polynomial encoding of these shifts, and we have
implemented a Toom-Cook 4-way generator
(\texttt{--algo-b toom4}) that matches the SABER C reference
byte-for-byte on pinned inputs. However, the universal
verification target ``schoolbook = Toom-Cook 4-way'' is
intractable for state-of-the-art SMT solvers at any feasible
budget. We measured 30-minute (1800\,s) timeouts on the
smallest sensible case ($N = 4$, single output coefficient,
comparison modulo $q = 2^{13}$): both Bitwuzla and our
procedure time out. SABER's algorithm is provably correct
modulo $q = 2^{13}$ but \emph{not} modulo $2^{16}$ (the
storage type); the deviation is masked by SABER's downstream
mod-$q$ reductions. The empirical reduction modulo $2^{13}$
holds across 100 random uint16 inputs at every
$N_{\mathrm{SB}} \in \{1, 2, 4, 8, 16, 32, 64\}$, but verifying
this universally is, on present evidence, beyond current
SMT capability. Faithful Toom-Cook 4-way SABER verification is
therefore an open challenge that motivates further work, both
in SMT-solver heuristics and in alternative procedure
architectures (e.g.\ specialised Toom-Cook structure
recognition, or decomposition into evaluation, multiplication,
and interpolation sub-lemmas).

\paragraph{Deferred bit-blasting.}
\label{sec:eval-saber-defer}
The headline numbers in Table~\ref{tab:saber-scaling} rely on
\emph{deferred bit-blasting}, an architectural choice in our
boolbv layer that defers the conversion of SSA equalities to
SAT clauses until after the algebraic procedure has had a
chance to refute. If the algebraic procedure succeeds (the
universal-equational common case for SABER), the queued
bit-blasting work is discarded and the SAT solver is invoked
only with the algebraic-induced \texttt{false} clause. If the
algebraic procedure does not refute, the queue is replayed
through the parent set\_to before the SAT solver runs,
recovering identical behaviour with no regression.

Without this deferral the procedure incurs $\sim$30\,GB peak RSS
at $N = 256$ from the SAT clauses generated for SABER's nested
$\mathtt{bvmul}$, $\mathtt{bvshl}$, $\mathtt{bvsub}$ SSA
structure, and runs out of memory at $N \geq 384$ within a
55\,GB virtual memory budget.

\begin{table}[ht]
\caption{SABER karatsuba2 with the default (deferred) configuration
versus the $\texttt{DISABLE\_DEFER\_BITBLAST}$ ablation. Per output
coefficient via the all-coefficients disjunction; 600\,s timeout;
55\,GB virtual memory limit. The ablation ceiling at
$N \geq 384$ is memory-bound (\textsc{oom}); the default ceiling
at $N \geq 1024$ is time-bound (\textsc{t/o}).}
\label{tab:saber-defer}
\centering
\small
\begin{tabular}{lrrrr}
\toprule
& \multicolumn{2}{c}{ablation: \texttt{DISABLE\_DEFER\_BITBLAST}}
& \multicolumn{2}{c}{default} \\
\cmidrule(lr){2-3}\cmidrule(lr){4-5}
$N$ & RSS (GB) & wall (s) & RSS (MB) & wall (s) \\
\midrule
64   & 1.95  & 6.7    & 22   & 0.3 \\
128  & 7.7   & 27.8   & 60   & 2.0 \\
256  & 30.8  & 117.6  & 206  & 13.4 \\
320  & 51.9  & 195.1  & 313  & 25.2 \\
384  & \multicolumn{2}{c}{\textsc{oom}}  & 445  & 42.6 \\
512  & \multicolumn{2}{c}{\textsc{oom}}  & 776  & 98.3 \\
768  & \multicolumn{2}{c}{\textsc{oom}}  & 1717 & 324.3 \\
1024 & \multicolumn{2}{c}{\textsc{oom}}  & 3030 & \textsc{t/o} \\
\bottomrule
\end{tabular}
\end{table}

Across $N \in \{64, \ldots, 320\}$ the default cuts memory by
100--165$\times$ and wall-clock time by 8--20$\times$ relative to
the ablation. Beyond $N = 320$ the ablation is unable to run
within the 55\,GB budget at all; the default reaches $N = 768$
in $\sim$5.8 minutes with under 2\,GB peak memory. At
$N = 1024$ the algebraic Buchberger phase becomes the dominant
cost; pushing the time-bound ceiling further would require
faster polynomial-arithmetic procedures, not more memory.

\paragraph{Reproducibility.}
Queries are mechanically generated from
\texttt{bench-multiplication/saber/make-saber-query.py}. SABER
reference code from the KU Leuven submission to NIST
PQC~\cite{KULeuvenSABER}.

\subsection{Custom Suite (39 benchmarks)}
\label{sec:eval-custom}

The custom suite covers five categories: commutativity and
associativity identities (15 benchmarks, BW=8 to 32),
overflow-checking and strength-reduction patterns (6), DSP
datapath equivalence benchmarks based on
Shekhar et al.~\cite{shekhar2007equivalence} (5), division and
roundtrip queries (4), and bfloat16/IEEE FP multiplication
queries (9).

\begin{table}[ht]
\caption{External solver comparison on the 39-benchmark custom
  suite. Times in seconds (median of 3 runs, 60\,s wall timeout).
  ``n.s.'' marks bfloat16 benchmarks that Bitwuzla and cvc5 reject
  in their default configurations as unsupported floating-point
  formats (both rely on SymFPU which excludes 8-bit exponent /
  8-bit mantissa types by default; experimental builds with
  \texttt{--fpexp} / \texttt{--fp-exp} would solve these).}
\label{tab:external}
\centering
\small
\begin{tabular}{lrrr}
\toprule
Benchmark & Bitwuzla & cvc5 & \cbmc \\
\midrule
\multicolumn{4}{l}{\emph{Commutativity, associativity, distributivity}} \\
comm\_8 & 0.002 & 0.006 & 0.005 \\
comm\_10 & 0.002 & 0.006 & 0.005 \\
comm\_11 & 0.002 & 0.006 & 0.005 \\
comm\_12 & 0.002 & 0.005 & 0.005 \\
comm\_13 & 0.002 & 0.005 & 0.004 \\
comm\_14 & 0.002 & 0.005 & 0.004 \\
comm\_15 & 0.002 & 0.005 & 0.004 \\
comm\_16 & 0.002 & 0.006 & 0.004 \\
comm\_18 & 0.002 & 0.005 & 0.004 \\
comm\_20 & 0.002 & 0.005 & 0.004 \\
comm\_24 & 0.002 & 0.005 & 0.004 \\
comm\_32 & 0.002 & 0.005 & 0.004 \\
assoc\_8 & 0.002 & 0.005 & 0.004 \\
distrib\_8 & 0.002 & 0.005 & 0.004 \\
\midrule
\multicolumn{4}{l}{\emph{Overflow detection \& strength reduction}} \\
overflow\_detect\_16 & 0.002 & 0.014 & 0.007 \\
add\_overflow\_16 & 0.002 & 0.006 & 0.005 \\
checked\_mul\_16 & 0.002 & 0.014 & 0.007 \\
add\_chain\_16 & 0.002 & 0.005 & 0.004 \\
add\_chain\_32 & 0.002 & 0.005 & 0.004 \\
add\_sub\_cancel\_16 & 0.002 & 0.005 & 0.004 \\
equiv\_unsat\_8add\_16 & 0.002 & 0.005 & 0.006 \\
strength\_chain\_16 & 0.002 & 0.008 & 1.49 \\
strength\_16\_15 & 0.002 & 0.007 & 0.005 \\
strength\_16\_31 & 0.002 & 0.007 & 0.005 \\
\midrule
\multicolumn{4}{l}{\emph{DSP datapath equivalence}} \\
dsp\_image\_reject & 0.005 & 0.011 & 0.015 \\
dsp\_horner\_16 & 0.002 & 1.84 & 0.010 \\
dsp\_vanishing\_poly\_8 & 0.011 & 0.048 & 0.008 \\
dsp\_vanishing\_mv & 0.002 & 0.006 & 0.005 \\
dsp\_coeff\_scale\_8 & 0.002 & 0.006 & 0.005 \\
\midrule
\multicolumn{4}{l}{\emph{Cryptographic \& division}} \\
mul\_ineq\_12 & 1.37 & 1.84 & 0.511 \\
div\_mul\_roundtrip\_12 & 5.18 & 6.19 & 4.06 \\
gf256\_mul\_assoc & 0.021 & 0.053 & 0.054 \\
crypto\_square\_mod & 0.012 & 0.088 & 0.019 \\
barrett\_red\_8 & 0.004 & 0.009 & 0.011 \\
fixedpoint\_mul\_16 & 0.004 & 0.013 & 0.024 \\
\midrule
\multicolumn{4}{l}{\emph{Bfloat16 multiplication}} \\
bf16\_mul\_comm & \multicolumn{1}{c}{n.s.} & \multicolumn{1}{c}{n.s.} & 0.006 \\
bf16\_mul\_comm\_v2 & \multicolumn{1}{c}{n.s.} & \multicolumn{1}{c}{n.s.} & 11.2 \\
bf16\_mul\_const & \multicolumn{1}{c}{n.s.} & \multicolumn{1}{c}{n.s.} & 0.028 \\
bf16\_mul\_mono & \multicolumn{1}{c}{n.s.} & \multicolumn{1}{c}{n.s.} & 29.5 \\
\midrule
Total solved (of 39) & 35 & 35 & \textbf{39} \\
\bottomrule
\end{tabular}
\end{table}

\cbmc solves all 39, while Bitwuzla and cvc5 each solve 35 (both
declining the same 4 bfloat16 benchmarks in default mode). On the
35 benchmarks all three solve, Bitwuzla is fastest in
the majority of cells (typically $\sim$2\,ms via word-level
rewriting), \cbmc is competitive ($\sim$5\,ms with the algebraic
layer), and cvc5 is slowest but consistent. The largest gap
appears on \texttt{dsp\_horner\_16}, where \cbmc's vanishing
polynomial test resolves the equivalence in 10\,ms while cvc5
requires 1.84\,s of bit-blasted reasoning. The two
benchmarks where \cbmc is noticeably slower
(\texttt{strength\_chain\_16}: 1.49\,s, \texttt{bf16\_mul\_mono}:
29.5\,s) are bit-blasted queries the algebraic layer cannot
shortcut.

\textbf{DSP benchmarks (cross-reference).} The 5 DSP benchmark
results in this table are reported alongside the dedicated DSP
discussion in Section~\ref{sec:eval-dsp}; the bias of this
subset toward our techniques is discussed in
Section~\ref{sec:threats}.

\subsection{SMT-COMP 2024 QF\_BV Community Sample (66 benchmarks)}
\label{sec:eval-smtcomp}

For a blinded community-benchmark evaluation we took a stratified
sample of 66 multiplication-containing benchmarks from the SMT-COMP
2024 QF\_BV track, drawn across 22 submitter categories (5 per
category where available). We ran \cbmc with its default
configuration (algebraic pre-solver + \combacs) and with the
algebraic pre-solver disabled (shift-add baseline), with a 60\,s
timeout.

\begin{table}[ht]
\caption{SMT-COMP QF\_BV stratified sample (66 benchmarks, 60\,s T/O).
  cvc5 run with default settings (its QF\_BV pipeline; the \texttt{--ff}
  flag applies only to the QF\_FF logic and does not activate on
  QF\_BV). Bitwuzla 0.9.0-dev added 2026-05-26.}
\label{tab:smtcomp-sample}
\centering
\begin{tabular}{lrr}
\toprule
Configuration & Solved & PAR-2 (s/benchmark) \\
\midrule
shift-add (bit-blasting only) & 34/66 & 63.1 \\
\cbmc default (algebraic + \combacs) & 33/66 & 63.5 \\
cvc5 default & 38/66 & 52.6 \\
Bitwuzla default & \textbf{52/66} & \textbf{28.7} \\
\bottomrule
\end{tabular}
\end{table}

On the stratified sample Bitwuzla solves the most benchmarks
(52), followed by cvc5 (38), shift-add \cbmc (34) and default
\cbmc (33). Bitwuzla's lead is consistent with its dominance on
the random-polynomial sample (Section~\ref{sec:eval-random-poly}):
its occurrence-map canonicalisation handles broad QF\_BV
queries that the algebraic pre-solver does not target.

Three benchmarks where default \cbmc times out but shift-add
succeeds are all \texttt{bvudiv}/\texttt{bvsdiv}-dominated: the
algebraic layer's GCD-based division-by-constant rewriting
expands the formula on these instances. Two further benchmarks
are solved by default \cbmc but not by shift-add (a rewrite-rule
family and \texttt{brummayerbiere3\_isqrtadd}); on three more,
default is ${\geq}$3$\times$ faster than shift-add (up to
3226$\times$ on the rewrite-rule family). No benchmark in the
sample is ${\geq}$3$\times$ slower with default than with
shift-add.

The relative ordering Bitwuzla $>$ cvc5 $>$ \cbmc on this
broader sample reflects mature word-level rewriting on queries
where multiplication is part of a richer structure
(non-polynomial operations, embedded multiplications); the
algebraic technique developed here is pattern-specific (the
universal-equational class with polynomial expressions in
$\mathbb{Z}_{2^d}$) and does not aim to replace mature SMT
solvers on general QF\_BV community
benchmarks (Section~\ref{sec:threats}).

\subsection{Partial Bit-Vector Identities via Bit-Decomposition}
\label{sec:eval-pbv}

The procedure described in Sections~\ref{sec:algebraic}
and~\ref{sec:vanishing} handles arithmetic operators
($+$, $-$, $\cdot$) and a few low-level operations that admit
direct polynomial encodings ($\mathtt{bvshl}\ a\ k$ for constant
$k$ encodes as $a \cdot 2^k$). Other ``partial'' bit-vector
operators---$\mathtt{bvlshr}$, $\mathtt{bvashr}$,
$\mathtt{bvand}$, $\mathtt{bvor}$, $\mathtt{bvxor}$,
$\mathtt{bvnot}$---do not admit such direct polynomial
encodings in $\mathbb{Z}_{2^d}$ alone. Without further
machinery, queries containing them fall through to bit-blasting
and the algebraic layer sees nothing.

We extend the procedure with \emph{bit-decomposition variables}:
for any expression $a$ of bitwidth $d$, we introduce $d$
auxiliary Boolean variables $b_{a,0}, \ldots, b_{a,d-1}$ with the
constraints
\begin{align*}
\text{(idempotency)} \quad & b_{a,i}^2 - b_{a,i} = 0
  \quad \text{for } i = 0, \ldots, d-1, \\
\text{(sum-decomposition)} \quad & a - \sum_{i=0}^{d-1} 2^i b_{a,i} = 0.
\end{align*}
In $\mathbb{Z}_{2^d}$, $b(b-1) \equiv 0$ implies $b \equiv 0$ or
$b \equiv 1$ (since $b$ and $b-1$ are coprime, one must be zero
modulo $2^d$); idempotency therefore restricts $b_{a,i}$ to
$\{0, 1\}$, and sum-decomposition forces it to be the $i$-th bit
of $a$. With bit variables in scope, the partial operators become
sound polynomial expressions, e.g.\
$a \gg k = \sum_{i=k}^{d-1} 2^{i-k} b_{a,i}$,
$\mathtt{bvand}(a, b) = \sum_i 2^i (b_{a,i} \cdot b_{b,i})$,
and so on.

\textbf{Implementation.} Bit-decomposition is applied
on demand: when extraction encounters a partial-bit-vector
operator, the operand is recursively bit-decomposed (with
constants and recognised bit-ops such as $\mathtt{bvnot}$
producing the bit polynomials directly without auxiliary
variables; otherwise a fresh ``host'' variable is introduced and
its bit decomposition added to the polynomial system). To keep
Buchberger tractable on these augmented systems, we apply three
optimisations: Frobenius-aware reduction (clamp $b^k$ to $b$ for
any bit variable $b$ and $k \geq 1$ during S-polynomial and
reduction steps); a polynomial-form host cache (compounds that
normalise to the same polynomial share a host); and linear
elimination of host variables (substitute $h \mapsto \sum_i 2^i b_i$
across the basis before Buchberger).

\textbf{Empirical evaluation.} We benchmark seven universal-
equational identities involving partial bit-vector operators
across bitwidths in $\{4, 16, 32, 64, 128\}$, comparing our
procedure with bit-decomposition (``Algeb'') against our
bit-blasting baseline (``BB''; same SMT2 entry, the algebraic
layer disabled by environment variable) and against
Bitwuzla. All queries are unsatisfiable; the assertion is that
some output bit differs.

\begin{table}[ht]
\caption{Partial bit-vector identities; runtimes in seconds at
60\,s timeout. ``Algeb'' is our procedure with bit-decomposition;
``BB'' is the same procedure with the algebraic layer disabled
(equivalent to bit-blasting alone); ``BW'' is Bitwuzla.}
\label{tab:eval-pbv}
\centering
\begin{tabular}{lrrrr}
\toprule
Identity & bw & Algeb & BB & BW \\
\midrule
$(a \oplus b) \oplus b = a$ & 16 & 0.00 & 0.00 & 0.00 \\
                            & 64 & 0.02 & 0.00 & 0.00 \\
                            & 128 & 0.06 & 0.00 & 0.01 \\
$\sim(a \wedge b) = \sim a \vee \sim b$ & 16 & 0.00 & 0.00 & 0.00 \\
                            & 64 & 0.02 & 0.00 & 0.00 \\
                            & 128 & 0.08 & 0.00 & 0.00 \\
$\sim\sim a = a$            & 16 & 0.00 & 0.00 & 0.00 \\
                            & 64 & 0.01 & 0.00 & 0.00 \\
                            & 128 & 0.04 & 0.00 & 0.00 \\
$(2x)\!\gg\!1 = x$ for $x < 2^{d-1}$ & 16 & 0.00 & 0.00 & 0.00 \\
                            & 64 & 0.01 & 0.00 & 0.00 \\
                            & 128 & 0.05 & 0.00 & 0.00 \\
$((a{+}b)\!\gg\!1) = ((b{+}a)\!\gg\!1)$ & 16 & 0.00 & 0.00 & 0.00 \\
                            & 64 & 0.01 & 0.01 & 0.00 \\
                            & 128 & 0.05 & 0.02 & 0.00 \\
$((a{+}b){-}(a{-}b))\!\gg\!1 = b$ for $b < 2^{d-1}$ & 16 & \textbf{0.00} & 0.04 & 0.00 \\
                            & 64 & \textbf{0.02} & 0.29 & 0.00 \\
                            & 128 & \textbf{0.07} & 0.61 & 1.24 \\
$((a{+}b){+}(c{+}d))\!\gg\!2 = ((a{+}c){+}(b{+}d))\!\gg\!2$
                            & 16 & 0.00 & 0.04 & 0.00 \\
                            & 64 & \textbf{0.02} & 0.30 & 0.00 \\
                            & 128 & \textbf{0.06} & 0.82 & 0.00 \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Observations.} Three patterns emerge from
Table~\ref{tab:eval-pbv}:

\emph{(1) Linear scaling in bitwidth.} Across all queries our
procedure scales linearly with $d$, consistent with the
bit-decomposition adding $d$ idempotency relations and a single
sum-decomposition equation per host. The cost of an additional
bit position is $\approx 0.5$\,ms at these scales.

\emph{(2) Bit-blasting is competitive at small bitwidths.} On
simple identities ($\oplus$ cancellation, De~Morgan,
involution), our bit-blasting baseline solves at any bitwidth in
$\leq 0.02$\,s---including queries that are within the algebraic
fragment with bit-decomposition. The algebraic procedure has
overhead in extraction and Buchberger that bit-blasting avoids;
on these queries the algebraic layer is correctly silent
(the procedure is sound but does not improve over bit-blasting).
Bitwuzla's word-level rewriting handles them in 0.00\,s as
expected.

\emph{(3) Bit-decomposition wins on toom-distrib.} The identity
$((a{+}b){+}(c{+}d)) \gg 2 = ((a{+}c){+}(b{+}d)) \gg 2$ is a
Toom-Cook-style algebraic re-arrangement under a shift. Here our
algebraic procedure with bit-decomposition is 4--14$\times$
faster than our bit-blasting baseline at $d \in \{64, 128\}$
(0.02--0.06\,s vs.\ 0.30--0.82\,s). Bitwuzla still wins
absolutely (0.00\,s) but the gap to our procedure is small
($<\!0.1$\,s at $d = 128$). The reason is structural: linear
elimination substitutes the inner sums $a{+}b$, $c{+}d$ with
their bit-sum forms once each, and Buchberger derives the
identity through bit-by-bit reduction without re-bit-blasting
the entire structure.

\textbf{Toom-scaled and bit-by-bit parity reasoning.} The
identity $((a{+}b){-}(a{-}b)) \gg 1 = b$ holds only when
$b < 2^{d-1}$ (the precondition prevents overflow in the
intermediate $2b$). The procedure must reconcile two facts:
the precondition (a $\mathtt{bvult}$ relation) forces the MSB
of $b$ to zero, and the bit-decomposition of $h = 2b \bmod 2^d$
must align position-by-position with the bits of $b$
($b_{h,0} = 0$, $b_{h,i+1} = b_{b,i}$ for $i < d-1$).

The bit alignment is not a polynomial consequence in
$\mathbb{Z}_{2^d}$: the polynomial relation
$\sum 2^i b_{h,i} = 2 \sum 2^i b_{b,i}$ alone admits multiple
solutions when interpreted bit-blindly, so Buchberger cannot
derive the alignment from idempotency and the host equation
alone. We materialise the alignment explicitly: when the
extractor produces a side equation $h - c x = 0$ with $c$ a
constant power of two and both $h$ and $x$ bit-decomposed, we
register the bit-position substitutions
$b_{h,i} \mapsto 0$ for $i < \log_2 c$ and
$b_{h,i + \log_2 c} \mapsto b_{x,i}$ otherwise. Linear
elimination then propagates the alignment through the basis
before Buchberger runs.

Combined with the $\mathtt{bvult}$ substitution
$b_{b,d-1} \mapsto 0$, this collapses the toom-scaled diff to
zero in the vanishing-polynomial test (revised toom-scaled row
in Table~\ref{tab:eval-pbv}: 0.00\,s at $d{=}16$, 0.07\,s at
$d{=}128$; previously T/O at $d \geq 32$). At $d{=}128$ our
0.07\,s beats Bitwuzla's 1.24\,s by 17$\times$.

\textbf{Universal-relational extension.}
The same bit-decomposition machinery extends to relational
predicates: an asserted $\mathtt{bvult}\ x\ C$ for a constant
$C = 2^k$ forces $b_{x,k}, \ldots, b_{x,d-1}$ to zero, which is
encoded as a substitution that propagates through the basis
during linear elimination. The dual case $x \geq 2^d - 2^k$
(common as the ``MSB-on'' precondition $x \geq 2^{d-1}$) forces
those same bits to one. General constants $C$ and
symbol-symbol comparisons fall through to a bit-comparator
chain encoding with $O(d)$ auxiliary bit variables, idempotency
relations, and definition polynomials.

\begin{table}[h]
\caption{Universal-relational predicates: representative
queries that combine a $\mathtt{bvult}$ / $\mathtt{bvuge}$
precondition with a polynomial body. Runtimes in seconds at
30\,s timeout. ``Algeb'' is our procedure with bit-decomposition
and the predicate extension; ``BB'' is the same procedure with
the algebraic layer disabled; ``BW'' is Bitwuzla.}
\label{tab:eval-pbv-relational}
\centering
\begin{tabular}{lrrrr}
\toprule
Identity & bw & Algeb & BB & BW \\
\midrule
$(\mathtt{bvult}\ x\ 2) \Rightarrow x \cdot x = x$
                            & 16 & 0.00 & 0.00 & 0.00 \\
                            & 64 & 0.03 & 0.01 & 0.00 \\
                            & 128 & 0.17 & 0.04 & 0.00 \\
$(\mathtt{bvult}\ x\ 4) \Rightarrow x(x{-}1)(x{-}2)(x{-}3) = 0$
                            & 16 & 2.09 & 0.02 & 0.00 \\
                            & 64 & 4.06 & 0.32 & 0.04 \\
                            & 128 & 8.39 & 1.81 & 0.02 \\
$(x \geq 2^{d-1}) \Rightarrow x \oplus 2^{d-1} = x - 2^{d-1}$
                            & 16 & 0.00 & 0.00 & 0.00 \\
                            & 64 & \textbf{0.01} & 0.00 & T/O \\
                            & 128 & \textbf{0.04} & 0.00 & T/O \\
\bottomrule
\end{tabular}
\end{table}

The first two queries are tractable for all three solvers; they
demonstrate the capability extension---without sub-goal 6 the
algebraic procedure could not reason about the precondition and
would fall through to bit-blasting. The third query exposes a
case where Bitwuzla's word-level rewriting times out at every
bitwidth tested ($\mathtt{bvxor}$ and $\mathtt{bvsub}$ do not
cancel under a universal-relational guard in the rewriter's
database), while our procedure refutes in 0.01--0.04\,s across
the full bitwidth range via direct bit-substitution combined with
the bit alignments materialised for the implicit $h = c x$ side
equation in the bit-decomposed expression.

\textbf{Implications.} Bit-decomposition extends the algebraic
procedure's \emph{capability} to partial bit-vector operators
and to relational preconditions, and (with bit alignments) to
shift-related identities, without changing its fundamental
architecture. The \emph{performance} contribution is narrow on
simple identities, where bit-blasting alone is competitive, but
the procedure now decides queries that mix polynomial structure
with relational guards and shift identities, including at least
one case (last row of Table~\ref{tab:eval-pbv-relational}) where
it succeeds 30--$\infty\times$ faster than Bitwuzla, and the
toom-scaled identity (Table~\ref{tab:eval-pbv}) where it
succeeds 17$\times$ faster at $d{=}128$. Signed comparisons
(\verb|bvslt|, \verb|bvsle|, \verb|bvsgt|, \verb|bvsge|) reduce
to unsigned via the standard sign-bit XOR transformation;
symbol-symbol comparisons use a bit-comparator chain encoding.

\subsection{Amulet2 Comparison}
\label{sec:eval-amulet}

Kaufmann and Biere's Amulet2~\cite{kaufmann2021amulet} verifies
gate-level multiplier circuits using Gr\"obner bases over
$\mathbb{Z}$ (with field polynomials $x^2 - x = 0$ to model
Booleans). Their tool works at the AIG level, not at the
SMT-LIB level; the natural comparison is scope rather than
head-to-head time.

Amulet verifies a specific multiplier implementation (the AIG
circuit it is given) against the integer multiplication
specification; we verify an SMT query involving one or more
uninterpreted \texttt{bvmul} calls. The two use cases overlap when
a verification problem can be phrased either way.

We ran Amulet2.1 (compiled from source, default configuration,
300\,s timeout) on the four AIG multiplier benchmarks shipped with
its artifact (\texttt{u\_4bp\_wt\_lf} at 128, 256, 512, 1024 bits;
Booth partial-product generator, Wallace-tree accumulation,
Ladner-Fischer final adder). Amulet times out on all four
benchmarks within the 300\,s budget. These are the larger
scalability benchmarks in the distribution; Amulet's own published
evaluation~\cite{kaufmann2021amulet} uses 64-bit multipliers
which verify in fractions of a second (${<}$1\,s per benchmark
according to the artifact's timing data). At 64 bits the AIG
structure is small enough that gate-level algebraic reasoning
works well; at 128+ bits the field-polynomial approach accumulates
symbolic overhead that the 300\,s budget cannot absorb.

Our Gr\"obner-basis solver, at an equivalent-sized problem (SMT
query $a \cdot b = b \cdot a$ at BW$=$256), reports the identity in
$<$5\,ms (Table~\ref{tab:scaling}). The two numbers are not
directly comparable: Amulet verifies a specific AIG
implementation, while we verify the abstract identity $a \cdot b = b \cdot a$
against itself, which our solver recognises algebraically. Amulet
would also handle this case in a few milliseconds at its natural
64-bit scale; beyond that, the field-polynomial cost dominates.

The difference in coefficient rings matters. Amulet uses
$\mathbb{Q}$ with field polynomials $x^2 - x$, which makes every
bit a field element but produces exponential polynomial blowup in
the worst case. We use $\mathbb{Z}_{2^d}$ with no field polynomials,
which avoids the blowup but loses field structure (division only
works for odd elements). This paper's empirical finding is that on
arithmetic-identity queries the $\mathbb{Z}_{2^d}$ approach is
dramatically faster because the unit-constant check terminates
quickly; on gate-level circuit verification we expect Amulet to
retain its advantage at modest bitwidths due to the completeness
of its field-based reduction.

\subsection{Threats to Validity}
\label{sec:threats}

\textbf{Benchmark selection.} The 39-benchmark custom suite is
biased toward multiplication-heavy arithmetic queries common in
cryptography and DSP but rare in typical pointer-safety checking.
We include the 66-benchmark SMT-COMP sample
(Section~\ref{sec:eval-smtcomp}) explicitly to
counter this bias. The community sample is where our approach
looks most neutral (matching shift-add).

\textbf{DSP benchmark design.} The 5 DSP datapath benchmarks in
the custom suite were designed by us to exercise the
vanishing-polynomial test; their inclusion shows the test works
as intended but should not be read as evidence of general
superiority. Our finding that all three tools solve these in
$<$50\,ms is therefore best interpreted as validating that the
test is correctly implemented rather than demonstrating a practical
advantage.

\textbf{Higher bitwidths.} The Gr\"obner-basis procedure scales
independently of bitwidth in principle
(Section~\ref{sec:scaling}). The community benchmarks include
up to 512-bit operands which our implementation does not yet
handle on every instance; understanding the remaining gap is
future work.

\textbf{Gate-level equivalence is a gap.} Our procedure is
complete for the universal-equational fragment with polynomial
expressions in $\mathbb{Z}_{2^d}$, but \emph{gate-level}
multiplier equivalence checking---the problem
Amulet2 (Section~\ref{sec:eval-amulet}) solves---sits outside
this fragment. A natural way to encode a gate-level circuit in
our setting is to add a field polynomial $x \cdot (x - 1) = 0$
for every bit variable, forcing it to be Boolean, and then to
check that the circuit's output polynomial equals the
specification. We
tested this on a small hardware-multiplier-equivalence benchmark
(\texttt{hw\_mul\_equiv}): at BW=2 the augmented system has
a strong Gr\"obner basis of 23 elements including a unit constant,
so we correctly report UNSAT. At BW$\geq$4, however, the basis
converges to 18 elements with no unit constant, and our procedure
returns ``unknown.'' This is consistent with the literature: the
gate-level approaches of
Biere, Kauers, and Ritirc~\cite{biere2017challenges} and
Kaufmann and Biere~\cite{kaufmann2021amulet} exploit the topology
of the AIG (reverse topological ordering of gate polynomials) to
avoid computing a full Gr\"obner basis in the first place; a
general-purpose Buchberger loop cannot reproduce that structural
shortcut without explicit circuit information. Bridging this gap
is a promising direction for future work (one could, for example,
extract and use the SSA topological order of \cbmc's bit-level
decomposition).

\textbf{No correctness regressions.} The 691 \cbmc regression
tests (covering array bounds, pointer safety, floating-point, and
other properties) all pass with the algebraic layers enabled. The
algebraic layers add $<$4\,ms of overhead on benchmarks that do
not exercise them.

\textbf{Interaction with bit-blasting.} This paper shares with the
bit-blasting companion paper~\cite{companion-bitblasting} the
observation that \cadical's
inprocessing BVE can be counterproductive for carry-save popcount
encodings (4--140\% overhead in one subset of benchmarks). In
practice the adaptive heuristic in the bit-blasting solver routes
such multiplications to shift-add when BVE would cascade
usefully.


\section{Mechanised Soundness Proofs}
\label{sec:lean4}

We verified the soundness of the algebraic Gr\"obner-basis solver
in Lean~4 (Lean 4.18.0, Mathlib v4.18.0), with bi-directional
traceability between the implementation and the proofs (recorded
in \texttt{formal-proofs/TRACEABILITY.md}). The mechanisation
covers the following modules:

\begin{enumerate}

\item \textbf{Unit characterisation in $\mathbb{Z}_{2^d}$}
  (\texttt{GroebnerSoundness.lean}): odd elements are units
  (coprime to $2^d$); 2 is not a unit for $d \geq 1$. (Submitted
  to Mathlib as PR \#38628.)

\item \textbf{Ideal preservation}
  (\texttt{BuchbergerCorrectness.lean}): S-polynomial computation
  and polynomial reduction preserve ideal membership; the basis
  $G$ produced by the algorithm satisfies $\langle G \rangle =
  \langle F \rangle$ for input $F$.

\item \textbf{Rabinowitsch soundness}
  (\texttt{GroebnerSoundness.lean}): unit in ideal $\Rightarrow$
  no solution, via ring homomorphism to a nontrivial ring.

\item \textbf{Buchberger termination}
  (\texttt{BuchbergerTermination.lean}): the ascending chain
  condition in Noetherian rings guarantees termination.

\item \textbf{Bit-decomposition soundness} (\texttt{Re4.lean}):
  the encoding emitted by \texttt{decompose\_bits} is sound. We
  prove (\emph{i}) that idempotency $b \cdot b = b$ in
  $\mathbb{Z}_{2^d}$ forces $b \in \{0, 1\}$; (\emph{ii}) the
  Frobenius identity $b^k = b$ for any idempotent $b$ and
  $k \geq 1$, justifying the exponent-clamping inside
  \texttt{polynomialt::multiply}; (\emph{iii}) that the standard
  binary expansion $\sum_i 2^i \,b_i = x$ satisfies the
  sum-decomposition equation. All three with zero
  \texttt{sorry}.

\item \textbf{Sub-goal 6 (universal-relational) encoding
  soundness} (\texttt{SubgoalSix.lean}): for the encoding
  strategies in \texttt{extract\_predicate}, we prove
  (\emph{i}) the constant power-of-2 upper bound: if
  $x.\mathit{val} < 2^k$ then bits $k..d-1$ of $x$ are zero
  (justifying the substitution emitted by
  \texttt{try\_upper\_bound});
  (\emph{ii}) the symmetric lower-bound case: if $x.\mathit{val} \ge 2^d - 2^k$
  then bits $k..d-1$ of $x$ are all one (justifying \texttt{try\_lower\_bound}'s
  MSB-on shortcut), proven by contradiction via bit-position
  decomposition;
  (\emph{iii}) the signed-via-XOR transformation: under the map
  $x \mapsto (x + 2^{d-1}) \bmod 2^d$, the signed comparison
  $\mathit{signedVal}\,a < \mathit{signedVal}\,b$ becomes the unsigned
  comparison $(a + 2^{d-1}).\mathit{val} < (b + 2^{d-1}).\mathit{val}$,
  proven by case analysis on whether $x.\mathit{val} < 2^{d-1}$.
  (\emph{iv}) The bit-comparator chain encoding: a Boolean
  recurrence \texttt{chainLtBool} mirroring the polynomial
  recurrence in \texttt{extract\_predicate}, with
  \texttt{chainLtBool\_correctness} proving it computes
  $(\mathrm{decide}(V_x < V_y), \mathrm{decide}(V_x = V_y))$ for
  bit lists, by induction on the lists with a per-Bool-pair case
  split. All four with zero \texttt{sorry}.

\item \textbf{Bit-alignment soundness} (\texttt{BitAlignment.lean}):
  for the substitutions emitted by \texttt{materialise\_bit\_alignments},
  we prove that $h = 2^k \cdot x$ in $\mathbb{Z}_{2^d}$ implies
  bits $0..k-1$ of $h$ are zero and bit $i+k$ of $h$ equals bit
  $i$ of $x$ for $i + k < d$. The proofs reduce to
  \texttt{Nat.testBit\_mod\_two\_pow} +
  \texttt{Nat.testBit\_mul\_pow\_two} (the latter from Lean
  core); both with zero \texttt{sorry}.

\item \textbf{Vanishing polynomial test soundness}
  (\texttt{Vanishing.lean}): we prove that the falling factorial
  $(x)_n$ is zero on every $x \in [0, n)$, and that a polynomial
  reducing to zero modulo the falling factorial vanishes on the
  same range. Both with zero \texttt{sorry}.

\item \textbf{Strong Gr\"obner basis 2-trick saturation}
  (\texttt{StrongGB.lean}): we prove that scalar multiplication
  preserves ideal membership (the 2-trick is sound, both for
  $\mathbb{Z}_{2^d}$ and for $\mathrm{MvPolynomial}\, \mathrm{Fin}\, n\, \mathbb{Z}_{2^d}$),
  and re-export the existing soundness chain (odd constant in
  basis $\Rightarrow$ ideal $= \top \Rightarrow$ original system
  UNSAT). We also prove:
  \begin{itemize}
  \item \texttt{naive\_completeness\_is\_false}: the naive
    completeness statement (``$F$ unsat $\Rightarrow$ odd constant
    in $\langle F\rangle$'') is \emph{false}; concrete
    counterexample with $d=2$, $n=0$, $F=\{C\,2\}$.
  \item \texttt{d\_eq\_one\_completeness}: for $d=1$ (over
    $\mathrm{GF}(2)$) with idempotency on each variable, $F$
    unsat implies $1 \in \langle F\rangle$. Proof via
    maximal-ideal argument + quotient field + idempotent
    $\Rightarrow$ $\{0,1\}$.
  \item \texttt{sq\_eq\_self\_of\_zmod\_two\_pow}: $x^2 = x$ in
    $\mathbb{Z}_{2^d}$ implies $x \in \{0,1\}$. Generalises
    \texttt{eq\_zero\_or\_one\_of\_sq\_eq\_self} to non-domains.
  \item \texttt{isLocalRing\_ZMod\_prime\_pow}:
    $\mathbb{Z}_{p^n}$ is a local ring for prime $p$, $n \geq 1$.
    Mathlib contribution candidate.
  \end{itemize}
  The deep completeness theorem
  (\texttt{two\_trick\_saturation\_complete}) is stated
  precisely with a \texttt{WellFormedEncoding} hypothesis
  (idempotency on each variable, placeholder for the full
  BV-encoding structure) but admitted as \texttt{sorry}.
  Two axioms (\texttt{strongGB} as
  the algorithm interface, \texttt{strongGB\_ideal\_preserved}
  as the soundness invariant) capture what the implementation
  provides. Full mechanisation of completeness is the
  Song et al.~\cite{song2024equational} contribution and
  requires the five-step decomposition documented in the
  \texttt{StrongGB.lean} module docstring.

\item \textbf{Deferred bit-blasting equivalence}
  (\texttt{Defer.lean}): the deferral mechanism in
  \texttt{boolbv.cpp::set\_to} / \texttt{finish\_eager\_conversion}
  is semantically equivalent to the eager path. We adopt an
  axiomatic semantic model: opaque types for solver state and
  assertions, with semantic operations (\texttt{setToEager},
  \texttt{setToDeferred}, \texttt{finishEagerConversion}) and
  two axioms (\texttt{defer\_finish\_eq\_eager\_finish}: a
  deferred assertion followed by replay equals an eagerly
  asserted one on the post-replay state;
  \texttt{finish\_eager\_commutes}: \texttt{setToEager} does
  not affect the deferred queue). From these, we prove
  \texttt{defer\_replay\_equivalence}: for any sequence of
  assertions, the deferred-then-flushed path yields the same
  state as the eager path. Proven with zero \texttt{sorry} by
  induction on the assertion list. Mechanising the two axioms
  themselves would require modelling the boolbv layer's
  operational semantics (estimated 1--2 weeks of follow-on
  work).

\end{enumerate}

\paragraph{Traceability.} Each Lean theorem carries an
\texttt{IMPL:} comment naming the source-file site whose soundness
it witnesses; each implementation site carries a \texttt{PROOF:}
comment naming the Lean theorem(s) that justify it. The full
mapping is in \texttt{formal-proofs/TRACEABILITY.md}.

\paragraph{Status summary.} Out of 16 mapping entries:
\begin{itemize}
\item 14 are fully mechanised (zero \texttt{sorry}, no
  module-specific axioms beyond mathlib).
\item 1 is mechanised relative to two explicit semantic axioms
  (\texttt{defer\_replay\_equivalence}, with axioms
  \texttt{defer\_finish\_eq\_eager\_finish} and
  \texttt{finish\_eager\_commutes} reflecting properties of the
  boolbv layer's pipeline that would require formalising its
  operational semantics to discharge --- estimated 1--2 weeks of
  follow-on work).
\item 1 is statement-only with documented decomposition
  (\texttt{two\_trick\_saturation\_complete} --- the Song
  et al.~\cite{song2024equational} completeness theorem for
  strong Gr\"obner bases over $\mathbb{Z}_{2^d}$, requiring an
  extension of mathlib's monomial-order division algorithm past
  its unit-leading-coefficient hypothesis plus a mechanisation
  of the 2-trick saturation step. Estimated 1--6 months of
  Lean work, comparable to a master's thesis).
\end{itemize}
All modules build cleanly with mathlib v4.18.0; CI integration is
straightforward. The two non-fully-mechanised entries have
precise Lean statements (not vacuous) and detailed module
docstrings explaining what a full proof would require.


\section{Related Work}

We organise related work by which corner of the four-classes
taxonomy (Table~\ref{tab:taxonomy}) each line of work attacks.

\paragraph{Universal-equational, ideal membership.}
Song et al.~\cite{song2024equational} prove that strong Gr\"obner
bases over $\mathbb{Z}_{2^d}$ solve the equational theory of
bit-vectors --- the theoretical foundation we build on.
Bryant~\cite{bryant1991complexity} earlier showed that BDD-based
multiplication verification requires exponential size, motivating
non-BDD algebraic approaches. Clegg
et al.~\cite{clegg1996groebner} proved the Gr\"obner proof system
can be exponentially more powerful than resolution, giving the
proof-theoretic justification for algebraic methods on
multiplication. Our contribution beyond Song et al.\ is the SMT
integration: SSA-aware decomposition of nested multiplications,
the Rabinowitsch trick for disequalities, progress-based
termination, and mechanised soundness
(Section~\ref{sec:algebraic-contrib}).

\paragraph{Universal-equational, function equivalence.}
Shekhar et al.~\cite{shekhar2007equivalence} characterise the
vanishing ideal of $\mathbb{Z}_{2^{n_1}} \times \cdots \times
\mathbb{Z}_{2^{n_d}} \to \mathbb{Z}_{2^m}$ using falling
factorials and give a complete decision procedure for polynomial
equivalence in this setting; G\`amez-Montolio
et al.~\cite{gamez2024efficient} give the efficient normalisation
algorithm (via Stirling numbers and Kronecker products) that we
implement. Howe et al.~\cite{howe2025subpolynomial} decompose
univariate polynomials into subpolynomials for compact
bit-blasting; the technique is orthogonal to our
vanishing-polynomial test but could combine with it on queries
that fall through to the bit-blast layer.

\paragraph{Universal-equational on gate-level circuits.}
Gate-level algebraic verification is a related but distinct line:
the input is an AIG circuit, not an SMT query. Biere, Kauers, and
Ritirc~\cite{biere2017challenges} observed that gate polynomials
form a Gr\"obner basis under reverse topological ordering,
yielding polynomial-time multiplier verification.
Kaufmann~\cite{kaufmann2020thesis} and Kaufmann and
Biere~\cite{kaufmann2021amulet} extended this to the Amulet2 tool
with XOR slicing and proof certificates (PAC/LPAC).
Yu et al.~\cite{yu2016arithmetic} achieve linear-time gate-level
verification via function extraction. These approaches work over
$\mathbb{Q}$ with field polynomials $x^2 - x = 0$ to model
Booleans; ours works directly over $\mathbb{Z}_{2^d}$, avoiding
the exponential polynomial blowup field polynomials cause but
losing field structure. Section~\ref{sec:eval-amulet} compares
in detail.

\paragraph{Existential-equational and finite-field
generalisations.}
\v{O}zdemir et al.~\cite{Ozdemir23cvc5}
integrate CoCoALib~\cite{abbott:cocoalib} into cvc5 for
Gr\"obner-basis reasoning over prime-order finite fields,
targeting QF\_FF queries (e.g., zkSNARK constraint systems);
their procedure handles both UNSAT and SAT directions via root
finding. The setting is different: prime-order fields admit
field structure and complete univariate root finding, while
$\mathbb{Z}_{2^d}$ does not. The two procedures are complementary
on cryptographic verification: finite-field methods excel on
curve-based cryptography, our approach on bit-vector arithmetic
identities. The cvc5 \texttt{--ff} flag activates this only for
QF\_FF logic; QF\_BV queries do not invoke it.

\paragraph{Heuristic word-level rewriting (covers all four
classes).}
Niemetz et al.~\cite{niemetz2023bitwuzla} implement occurrence-map
canonicalisation in Bitwuzla that handles commutativity and
associativity via syntactic rewriting; cvc5's QF\_BV pipeline
applies pattern-matched rewrites and a Gaussian-elimination
preprocessor for linear systems modulo $2^d$. Both approaches are
\emph{heuristic} in our sense: they decide the queries that match
their pattern set or admit a linear reduction, and fall through
to bit-blasting on queries that escape. They cover all four
taxonomy classes when the rewriter has matching patterns; on
queries with high-degree polynomial structure or vanishing
polynomials they fall through. Our procedure is complementary:
on the universal-equational class with polynomial expressions in
$\mathbb{Z}_{2^d}$, it is a decision procedure rather than a
heuristic; on the other three classes it falls through to
bit-blasting in turn.

\paragraph{Relational classes.}
The bit-blasting companion paper~\cite{companion-bitblasting}
addresses the universal-relational and existential-relational
classes more uniformly than our procedure does. Its
multiplier-encoding contributions are orthogonal to the present
work: the algebraic procedure decides queries that escape any
multiplier encoding, while the bit-blasting companion paper
improves the encoding for queries that reach the SAT solver.

\paragraph{Proof complexity.}
The extended Frege proof system (including Gr\"obner-basis
reasoning) has polynomial proofs of arithmetic identities such
as commutativity~\cite{buss1995bounded}, a fact we rely on
implicitly: our algebraic procedure constructs such a proof.
Whether resolution also has polynomial proofs of
these identities is open~\cite{biere2017challenges}; the
bit-blasting companion paper~\cite{companion-bitblasting}
provides empirical evidence that it does not (integer
commutativity is $1000\times$ harder than GF(2) in CDCL).


\section{Conclusion}

We presented a decision procedure for universally-quantified
equalities of polynomial expressions in $\mathbb{Z}_{2^d}$,
combining a strong Gr\"obner-basis solver for ideal membership
with a vanishing-polynomial test for function equivalence over
the bit-vector domain. The procedure is the first SMT integration
of a complete decision procedure for this fragment; its
contribution is not to replace mature word-level SMT solvers on
volume but to decide queries those solvers cannot decide at all
--- 4 random-polynomial benchmarks at degree $\geq 13$ that
Bitwuzla, cvc5, and every bit-blasting configuration we tested
time out on, and 5 DSP datapath equivalences with overflow
cancellation that are decided in microseconds. Soundness is
mechanised in Lean 4 with 25 theorems and zero \texttt{sorry};
the formalisation effort exposed and removed a 2{,}000$\times$
ordering sensitivity in the C++ implementation that empirical
testing did not catch.

The procedure addresses the universal-equational class with
polynomial expressions; queries with bitwise operations, shifts,
or inequalities fall through to the bit-blasting companion
paper~\cite{companion-bitblasting}. Two extensions are sketched
in Section~\ref{sec:future-within-and-beyond}: expression-level
normalisation deepens the procedure's interaction with the SAT
search within the same class, while bit-decomposition variables
give a route to extending the algebraic layer's coverage to the
relational classes that currently fall through.

\paragraph{Declaration on Generative AI.}
During preparation, the authors used an AI coding assistant to
assist with implementation, experimentation, and drafting. All
results were verified by the authors, and the final paper was
reviewed and edited by the authors.

\paragraph{Data availability statement.}
All experimental data, benchmarks, and reproduction scripts are
available in the artifact accompanying this paper, in
\texttt{doc/paper-algebraic/data/} and
\texttt{bench-multiplication/}, together with a Makefile that
rebuilds the paper and a \texttt{REPRODUCE.md} that describes how
to re-run each experiment. Mechanised Lean 4 proofs of soundness
are distributed with the artifact and submitted as PR \#38628 to
Mathlib.

\bibliographystyle{splncs04}
\bibliography{references}

\end{document}
